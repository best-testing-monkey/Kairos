# Handoff — 2026-08-21 22:29

## TL;DR

Continuing from `docs/handoff-202608212029.md` (read that one for the 2-week replay results and the "raw signals lose money, needs a selection pass" reframing — this doc is an addendum, not a replacement). No code changes this segment — pure analysis and one background sweep in progress. Baz pushed back constructively on the pessimism again ("last time on 1d we had to make a selection to turn profits"), asked for a per-combo spreadsheet (delivered), then asked to extend the analysis to the full 3-month window to check which winners persist. Along the way, answered two sharp follow-up questions precisely: (1) why only 4 equity models exist in today's batch at all (answer: only 8 equities in the whole 57-ticker universe ever had matching oracle+base 1h data, all 8 were processed), and (2) found a real, not-yet-confirmed lead on why equity signals never appeared in the 2-week replay at all (a date-truncation bug candidate in `fetch_data_raw`). **A ~3-hour background sweep is currently running** to extend `signals_cache` coverage to the full 3-month accept-gate window; nothing else is blocked, this is the main thing to check on.

## Suggested skills

None needed. Plain SQL analysis + waiting on a background sweep.

## What was done this session (since the 20:29 handoff)

**Delivered the per-combo spreadsheet Baz asked for** — `kairos_1h_combo_stats.xlsx` (two sheets, Base and Finetuned, sorted best-to-worst, green/red row highlighting by sign), sent via `SendUserFile`. Source data: the same `papertrade_signals`/`papertrade_signals_closure` join from the previous segment, over the 2-week window (2026-08-07 to 2026-08-21). Build script: `/tmp/.../scratchpad/build_combo_spreadsheet.py` (scratchpad, may not survive — trivial to reconstruct: group by strategy_name/ticker/model_label, compute n/win%/avg%/sum%, split into two sheets).

**Investigated whether `--signal-selection` could express "keep only these winning combos"** — checked `strategy/signal_selection.py`'s grammar: no `IN (...)` operator, no OR-across-conditions support, and critically **no "Model" column at all** (can't distinguish Base vs Finetuned within the DSL). Concluded the DSL isn't expressive enough for an exact-whitelist inclusion rule; the plan going forward is to filter the precomputed signal tables directly via SQL rather than fight the DSL.

**Baz asked to extend to a full 3-month backtest of the profitable combos to check persistence.** Launched a background sweep extending `signals_cache` coverage from the already-done 2 weeks (Aug 7-21) back to the full accept-gate test window start (2026-05-23) — `/tmp/.../scratchpad/populate_signals_cache_1h.py`, modified to take an explicit `START_OVERRIDE`/`END_OVERRIDE` covering 2026-05-23 to 2026-08-07 (1843 additional hourly steps). **This is the main in-flight background task — see Open threads.** Unlike the 2-week run, this window is virgin territory for the prediction cache (never touched before), so it's proceeding at a similar ~1.7-2s/step pace but over ~5.5x more steps — expect roughly 3+ hours total, started 22:10:52.

**Baz asked "where are all non-crypto instruments among the signals?"** — sharp catch: every ticker in the 2-week combo tables was crypto; the 4 equity finetuned models (CRM, ABBV, CB, AAPL) never appeared at all. Traced precisely:
- Confirmed via direct SQL: zero rows in `signals_cache` even mention these 4 tickers — they never got generated in the first place, not lost downstream.
- Confirmed via log inspection: no exceptions, no skip messages either — total silence, consistent with `run()`'s Pass-1 per-group `except Exception: failures.append(...); continue` swallowing a failure into a list that my driver (`return_rows=False`) never captures or prints.
- Tested `fetch_data_raw()` directly for these 4 tickers at a real trading-hour timestamp: **no exception**, returns data fine — but the last available bar was suspiciously stale (`2026-08-14 04:00:00` for an `as_of` query at `15:00` the same day).
- Found the likely mechanism: `fetch_data_raw()` (`strategy/kairos_strategies.py:182`) does `end_dt = as_of.date()` — **discards the time-of-day** before querying `price_cache.get_price_data()`. This is fine for daily-or-coarser intervals but could cause an intraday equity fetch to miss same-day bars. Confirmed via direct `price_cache.get_price_data()` call that real, non-stale equity 1h data *does* exist through 2026-08-20 — so the raw data is there, something in `fetch_data_raw`'s handling of it for equities specifically isn't surfacing it. **Not fully root-caused** — crypto uses the exact same function successfully, so this is likely an interaction with equities' sparse trading-hours-only calendar, not a universal bug. Offered to chase this down further; Baz hasn't yet said whether to prioritize it.

**Baz then asked "4 equity models?! where are the hundreds of others?!"** — investigated and found a clean, complete, reassuring answer (not a bug): of the full 57-ticker equity list in `CANDIDATE_UNIVERSE`, only **8 tickers ever had both oracle_results AND model_results (stage=base) at 1h under the exact same asset grouping** — a hard requirement for `select_finetune_candidate` to consider a profile at all (`profiles` are built from `oracle_results`; `base_exists` is a secondary gate against `model_results` requiring an identical `assets` string match). Those 8 are AAPL, ABBV, ABT, BA, CB, COST, CRM, DE — **and all 8 were processed in today's batch** (4 accepted, 4 rejected) — nothing was missed or skipped. Checked a specific candidate set (AMD, AVGO, GOOGL, HD, JNJ — which have base data but showed up as candidates in an earlier count) and confirmed they have **zero** oracle_results rows at 1h at all, explaining their absence cleanly. The other ~49 equities in the universe were never oracle/base-tested at 1h in the first place — this is the same "the real discovery sweep hasn't been run yet" story from earlier in the day, not a new problem.

## Published artifacts

None via the Artifact tool. One file delivered directly to Baz this segment: `kairos_1h_combo_stats.xlsx` (via `SendUserFile`, not a hosted artifact — it's a local file transfer).

## Git state

Branch `master`, HEAD `68c10db` — unchanged since the 19:32 handoff, no commits this segment (pure analysis + one background data-population script, no code changes to the repo itself). Working tree has the same accumulating untracked handoff docs (now 9) plus this one — strongly worth pruning once Baz has reviewed the whole day.

## Concluded threads

- The spreadsheet request — delivered.
- The "why only 4 equity models" question — fully answered, not a bug, the universe of eligible equity candidates was always just 8.
- The `--signal-selection` DSL expressiveness check — concluded it can't do exact-whitelist filtering; decided to use direct SQL instead going forward.

## Open threads / possible next steps

**1. The 3-month `signals_cache` extension sweep is running right now — check on it first.** Started 22:10:52, background process (check via `ps aux | grep populate_signals_cache_1h` or `tail -f data/signals_cache_populate_1h.log`). As of this handoff: 123/1843 steps done (~6.7%), 0 failures, on pace for ~3+ hours total (finish estimate roughly 01:00-01:30). Once it completes: **re-run `--precompute` over the full window** (`--start 2026-05-23 --end 2026-08-21 --interval-ladder 1h`), then pull the same per-(strategy,ticker,model) breakdown query (see the 20:29 handoff for the exact SQL) restricted to the whitelist of combos that were profitable in the 2-week check, to see which ones stay winning over the full 3 months — this is the concrete next analytical step Baz asked for, not yet done because the data isn't there yet.

**2. The equity staleness bug lead is unconfirmed and unresolved.** `fetch_data_raw`'s `as_of.date()` truncation (`strategy/kairos_strategies.py:182`) is the prime suspect for why equity signals never got generated in the 2-week sweep, but the exact mechanism (why crypto is unaffected by the same code) isn't nailed down. Worth a dedicated investigation session if Baz wants equity coverage in the persistence analysis — right now the 3-month sweep in progress will almost certainly have the exact same equity-blindness problem, meaning the "which stay winning" analysis will likely be crypto-only again unless this gets fixed first. **Consider whether to pause/fix this before trusting the 3-month sweep's completeness for equities**, or accept the 3-month check as crypto-only for now and revisit equities separately.

**3. JTO-USD still needs a manual re-queue** (unchanged, low priority, several handoffs old now) — `--stage finetune_next --assets JTO-USD --interval 1h --backtest_period 3m --ft_epochs 30 --ft_batch_size 32 --skip-idle-check`.

**4. Everything else from the 19:32/20:29 handoffs is unchanged**: PREDCACHE-01 ticket unimplemented, `viability_report`/`finetuned_models` registry drift (ZW=F affected) not investigated further, Epic E17 not started, the actual discovery sweep deliberately deferred, standing terminology note (Baz is 44, 1990s-2000s computing terminology).

**5. Housekeeping**: 9 handoff docs accumulated today. Prune all but this one once reviewed.
