# Handoff — 2026-09-06 15:13

## TL;DR

Continues directly from `docs/handoff-202609061158.md`, written earlier in
this same session (covers the framework build + first 8 exchange docs —
not repeated here). Everything below happened after that doc was written,
and it's **the first time this framework produced real, live-measured
results instead of research** — 2 of the 8 documented exchanges (Saxo,
OKX) were actually signed up for and hit with real API calls.

**Headline results:**
- **Saxo Bank: LIVE-TESTED AND RULED OUT.** Two real `precheck` calls
  (1 AAPL share, 1 IWDA ETF unit) both confirmed a flat ~€12-13 commission
  per trade — worse than IBKR's $1 floor, not a fix for it. No longer a
  live equities candidate.
- **OKX: LIVE-TESTED AND CONFIRMED VIABLE.** Real fees: 0.20% maker /
  0.35% taker (EEA entity — notably higher than the 0.08%/0.10% found for
  global OKX in earlier research, same "separate legal entity, separate
  schedule" pattern as Bybit EU). No flat floor. Demo API key confirmed
  environment-locked (safe even with broader-than-requested Trade/Withdraw
  permissions). **OKX is the only exchange fully connected and validated
  so far.**
- **Bybit EU's spot margin mechanism confirmed first-party**: Baz pasted
  the actual page content after 4 automated fetch attempts (WebFetch,
  Playwright) all failed with bot-blocking symptoms. It's a real
  collateralized-borrowing system (per-coin interest rate + tiered
  collateral ratio), not the "10x leverage toggle" secondary press
  coverage implied.
- **OKX universe coverage measured**: 54/62 (87%) of Kairos's crypto
  universe has a direct EUR pair on OKX; 8 confirmed genuinely absent
  (not a quote-currency artifact — checked all 12 OKX quote currencies
  including USD/USDT/BTC, and all instrument types). 38 new candidates
  found (OKX-listed, not in Kairos's universe, clearing Kairos's own
  current volume floor) — saved to a new `docs/universe-expansion-
  candidates.md`, notably including **TRX (TRON)**, a top-15 coin
  missing from the universe.
- **All 8 exchange candidates screened for Israeli ownership** (Baz
  excludes Israeli companies — Plus500 was already ruled out on this
  basis, and he'd forgotten it applied here too). None are Israeli;
  founder/origin table saved in `docs/exchanges/README.md`.
- **Real-world signup latency now tracked for every candidate**: Bybit EU
  and Bitstamp both gate on real ID verification (~3 days) despite being
  "free" (no funded account needed) — those turned out to be different
  properties. Saxo and OKX both went from signup to live API calls
  same-day.

**No code changed this session-portion** — everything above is docs +
memory (`scripts/exchange_probe.py` and the ibkr refactor were the
earlier framework-build portion, already committed and described in the
prior handoff). 11 commits total across the whole session
(`c7442ee`..`737fce3`), all on `master`, **none pushed**.

**Credentials handling**: real API credentials (Saxo app key/secret/OAuth
token, OKX API key/secret/passphrase) were pasted into the conversation by
Baz and used live for validation calls. Verified after each use that
nothing leaked into any committed file (`grep` swept before every commit
touching the relevant docs). Never written to disk outside a throwaway
scratchpad script using environment variables. This pattern should repeat
for Bitstamp/Bybit EU once their verification clears — see the playbook
note in "Open threads."

## Suggested skills

- **`read-handoff`** to pick up context (this doc + `docs/handoff-202609061158.md`
  for the framework-build portion).
- **`cheap-models-first`** before any subagent spawn.
- If Bitstamp or Bybit EU verification clears and Baz wants the same
  live-validation treatment OKX/Saxo got, no new skill needed — just
  follow the pattern in this doc's "Open threads" item 1.

## What was done this session

*(Continuing from `docs/handoff-202609061158.md` — this section covers
only what happened after that doc was written.)*

1. **Bybit EU margin data confirmed first-party.** Baz linked Bybit EU's
   own margin-data announcement page. 4 automated fetch attempts failed
   (`WebFetch`: HTTP 429 then timeout; Playwright headless, twice, with
   HTTP2 disabled on the second try: protocol error then full timeout) —
   consistent with active bot-blocking. Baz pasted the actual rendered
   table content directly (42 coins: borrow rates, collateral ratios).
   Corrected the doc's earlier secondary-source "10x leverage" framing
   with the real mechanism. Committed `780d2d6`, `056370c`.

2. **Real signup latency recorded for Bybit EU and OKX** (`3c6605e`) —
   Bybit EU gates on ~3 days ID verification + the already-documented 48h
   API-key wait; OKX's initial prep step reported as ~5 minutes. Split
   `docs/exchanges/README.md`'s "cheapest to smoke-test" (needs no funded
   account) from "fastest to get access" (real verification latency),
   since Bybit EU's experience showed those aren't the same thing.

3. **Saxo Bank live-tested via real SIM credentials Baz provided**
   (App Key/Secret, OAuth endpoints, then a 24h access token) —
   credentials used only as environment variables to a throwaway
   scratchpad script, never written to any repo file. 4 real API calls:
   auth check (`/port/v1/users/me`), instrument search
   (`/ref/v1/instruments`), and 2 cost-check calls
   (`POST /trade/v2/orders/precheck`, one stock one ETF). Both cost
   checks landed at the same ~€12-13 flat commission, confirming (not
   just "likely") Saxo shares — and worsens — IBKR's flat-floor problem.
   Also found: `get_reference_price` returns `200` but empty Bid/Ask on a
   fresh SIM account (`MarketDataViaOpenApiTermsAccepted: false` in the
   user profile, an unaccepted-terms gate, not a bug). Committed `d8cb93b`.

4. **OKX live-tested via real demo credentials Baz provided** (API key,
   secret, passphrase — noted the key had broader permissions than the
   requested read-only, Trade+Withdraw included; confirmed this is safe
   because demo keys are environment-locked, verified directly: same
   key+demo-header returns a fake ~€98.8k balance, the identical call
   *without* the header is flatly rejected with
   `401 {"code":"50101","msg":"APIKey does not match current environment."}`).
   Hit a real bug along the way: the key failed with a misleading
   `401 {"code":"50119","msg":"API key doesn't exist"}` on `www.okx.com`
   — root cause found via web search: EEA-registered accounts must call
   **`eea.okx.com`**, not the global host; public endpoints work fine on
   either host, which hid the cause until an authenticated call was tried.
   Confirmed real fees (0.20%/0.35%, EEA-specific, higher than the
   0.08%/0.10% found for global OKX in research) and the fee sign
   convention (negative = you pay, positive = rebate — easy to get
   backwards). Committed `0c653b9`.

5. **OKX universe coverage measured** against Kairos's 62-symbol crypto
   universe (`kairos_pipeline.CANDIDATE_UNIVERSE["crypto"]`), using
   OKX's public instrument list (no auth needed) — 54/62 (87%) have a
   direct EUR pair, 8 confirmed genuinely absent. Baz asked twice whether
   a different quote currency (USD, then BTC) might recover any of the
   8 — checked explicitly both times: no, they have zero listing under
   any of OKX's 12 quote currencies and don't appear under any instrument
   type (spot/margin/swap/futures) at all. Committed `397944e`, `b1f4f3a`.

6. **New candidates found and saved.** Baz asked what could be added to
   the universe from OKX; rather than dumping all ~213 non-overlapping,
   non-stablecoin OKX assets, filtered to those clearing Kairos's own
   current lowest-volume crypto entry (JTO, ~8.1k EUR/24h) as a
   non-arbitrary floor — 38 qualify. Created
   `docs/universe-expansion-candidates.md` (new file) to hold this and
   future exchanges' candidate sections as they get connected; linked to
   the existing `project_crypto_commodity_universe_expansion` memory
   (the open thread this directly serves). Committed `a70ea10`.

7. **Israeli-ownership screen applied to all 8 candidates.** Baz asked
   directly whether Plus500 was acceptable, was reminded it's already
   excluded (Israeli company, ruled out 2026-08-25), then asked to check
   the new 8 for the same issue — he'd forgotten the criterion applied
   broadly, not just to Plus500. Researched founder/origin for all 8;
   none are Israeli. Saved as a permanent section in
   `docs/exchanges/README.md` so future candidates get the same screen.
   Committed `4657114`.

8. **Bitstamp signup started, latency recorded.** Baz began Bitstamp's
   real signup; ID verification is in progress, reported as up to 3 days
   — same latency class as Bybit EU despite the "no funded account
   needed" framing. Updated `docs/exchanges/bitstamp.md`,
   `docs/exchanges/README.md`, and memory. Committed `737fce3`.

## Published artifacts

None this session (no Artifact tool publishes) — all output is
repo-committed markdown, listed above.

## Git state

```
737fce3 Record Bitstamp's real signup latency (up to 3 days, ID verification)
4657114 Screen all 8 exchange candidates for Israeli ownership
a70ea10 Start a universe-expansion candidate list, sourced from OKX
b1f4f3a Confirm the 8 OKX gaps aren't a EUR-quoting limitation
397944e Check OKX's universe coverage against Kairos's crypto candidates
0c653b9 Confirm OKX EEA fees and demo-key safety with a live test
d8cb93b Confirm Saxo Bank fails the flat-floor check IBKR failed, worse
3c6605e Record real signup latency for Bybit EU and OKX
056370c Confirm Bybit EU margin mechanism with first-party page content
780d2d6 Add Bybit EU spot margin (Fullstock/UTA) finding
a6963e7 Document 3 more exchange candidates, add a docs/exchanges template
c7442ee Add a reusable broker/exchange cost-discovery probe framework
```

`git status -sb`: `master...origin/master/master [ahead 33]` — 11 new this
session-portion (`780d2d6`..`737fce3`) plus the earlier `c7442ee`/`a6963e7`
from before `docs/handoff-202609061158.md`, plus 21 pre-existing from
before this session. **Not pushed.** Remote quirk unchanged: the remote is
*named* `origin/master`, so `git log origin/master..HEAD` returns nothing
— use `git status -sb`.

Tree clean except the untracked `docs/handoff-*.md` pile (35 files now,
unpruned across many sessions — same standing housekeeping item every
recent handoff has noted).

No tests were run this session-portion (docs/memory-only changes, no code
touched since `a6963e7`).

## Concluded threads

- **Bybit EU margin mechanism** — confirmed first-party, corrected in docs.
- **Saxo Bank fee viability** — settled, ruled out with real numbers.
- **OKX fee viability + connectivity** — settled, confirmed viable and
  working end-to-end.
- **"Can we scrape/map OKX's instrument coverage against our universe?"**
  — done, 87% coverage measured, both follow-up quote-currency questions
  (USD-family, then BTC) answered definitively.
- **"What can we add from OKX?"** — 38 candidates found and saved.
- **Israeli-ownership screen for the 8 new candidates** — done, all clear.
- **Plus500 acceptability question** — answered (already excluded,
  unchanged).

## Open threads / possible next steps

**1. Keep validating exchanges as verification clears.** Bybit EU and
   Bitstamp are both mid-ID-verification (~3 days each, per their own
   reported status). When either clears: repeat the OKX/Saxo pattern —
   get real API credentials from Baz, validate via a throwaway scratchpad
   script (env vars only, `grep` the repo for leaked secrets before every
   commit), confirm demo/sandbox environment-locking before trusting any
   broad permissions, update the relevant `docs/exchanges/*.md` and
   `docs/exchanges/README.md`'s index/latency notes, update memory.
   Kraken and Bitvavo remain lower-priority (no self-service sandbox,
   need a real funded account first).

**2. A real full unit test suite run flagged 9 pre-existing failures,
   never resolved.** From `docs/handoff-202609061158.md` (carried
   forward, not touched this session-portion either): `pytest
   tests/unit/ -q` (53 minutes, unrelated to any broker-probe work)
   found 9 failures, all in `test_kairos_papertrade_leverage_regression.py`,
   confirmed to predate this session (last touched by `34adf28`, the
   commit right before this session started — the margin/currency-
   correction work). Test names match exactly the three "subtle bugs"
   CLAUDE.md's MTM margin section documents as already-fixed
   (same-day round-trip handling, admission-check margin counting, stale
   bracket orders) — looks like a possible regression from the
   pre-session margin work, or just stale test expectations against the
   now-corrected `margin_ibkr.yaml` figures. Offered to investigate,
   never got a decision — **explicitly ask again next session** rather
   than assuming it's been dropped.

**3. Formalize OKX (and eventually others) into the actual
   `ExchangeProbe` framework.** Everything measured this session was via
   one-off scratchpad scripts, not `scripts/okx_instruments.py`. Building
   the real, `docs/playbooks/add-exchange-probe.md`-conformant script
   would make the OKX universe-coverage sweep (and future re-checks)
   persisted/resumable via the shared `sweep_simple()`/DB machinery
   instead of a throwaway script each time. Not started — worth doing
   once at least 2-3 exchanges are validated, so the abstraction is
   proven against more than one real implementation.

**4. Run the deduped-group re-screening pipeline against
   `docs/universe-expansion-candidates.md`'s 38 OKX candidates** (and
   whatever Bitstamp/Bybit EU add later) — per
   `project_crypto_commodity_universe_expansion` memory's own stated
   order: universe screening → correlation/grouping → oracle + naive →
   base → small/mini → regenerate `docs/papers/`. Nothing in the
   candidate doc has gone through this yet; it's explicitly a sourced
   list, not an addition to `CANDIDATE_UNIVERSE`. **TRX (TRON)** was
   flagged as worth checking first — a top-15 market-cap coin missing
   from the universe for no identified reason.

**5. Whether IBKR is worth continuing to build against at all** —
   carried over from the previous handoff, now even more pointed: OKX is
   fully validated and cheaper, several more candidates are close behind.
   Still just a conversation-starter, not a coded task — raise it
   directly rather than defaulting back to IBKR-specific work.

**6. The `phantom_ledger` commission bug** — carried over, still
   unfixed, still undecided who fixes it (see `docs/handoff-202609061128.md`
   for the full description). Lower urgency given IBKR itself looks like
   the wrong venue regardless, per item 5.

**7. The naive class gate** — carried over from
   `docs/handoff-202609021247.md` via both intermediate handoffs, still
   not landed, still fully unblocked. Full description in that handoff;
   unchanged since.

**8. Inherited from earlier handoffs, still untouched**: small/mini model
   sweeps to match base's coverage; ~14 groups failing on the partial-bar
   NaN on the model path specifically; the stashed chunked-sampling VRAM
   prototype; wave packing committed but never run live; offline signal
   replay for selection/allocation tuning (deferred, not started); the
   long-dormant set (DEGIRO/BUX account signup, IB Gateway login,
   PREDCACHE-01, Epic E17, `n0`/`min_n` as global constants); housekeeping
   (35 unpruned handoff docs — now four sessions running on this note;
   `/tmp/papertrade_6m/`, `/tmp/naive_pt/`, `/tmp/pipe_wiring_test.db` —
   check whether these still exist before assuming they do).
