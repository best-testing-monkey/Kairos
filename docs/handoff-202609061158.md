# Handoff — 2026-09-06 11:58

## TL;DR

Continues directly from `docs/handoff-202609061128.md`, written earlier in
this same session (that doc's "What was done" covers the IBKR-commission
recall + the original 5-exchange framework build; not repeated here). After
that handoff was written, Baz asked to keep finding more broker/exchange
candidates, then asked for a template doc so future exchange docs follow a
fixed structure. Both done:

- **3 more exchanges documented**: Saxo Bank (equities), OKX, Bitstamp
  (crypto) — same pattern as the original 5 (parallel forks, each
  researching one exchange). **8 total now.**
- **`docs/exchanges/README.md`** — new: an index/comparison table across
  all 8 candidates AND the 10-point content template every doc in that
  directory must follow, written *before* the 3 forks so they had something
  concrete to follow instead of the structure being re-explained per-prompt.
- Two real findings synced back into `docs/broker-api-interface.md` and
  `roadmap/phase-5-auto-execution.md`: **Saxo's cost-discovery mechanism is
  a second Pattern A example** (a hybrid — publishes a rate card but needs
  a dry-run `precheck` call for the exact number, same shape as IBKR's
  `whatIfOrder`), and **Saxo likely shares IBKR's flat-per-trade-commission
  problem** despite having the best sandbox of any candidate surveyed
  (free, self-service, no KYC). OKX and Bitstamp are both cleanly Pattern B,
  cheap fees, free demo/sandbox, no flat floor.

**New standing preference saved this session, not project-specific**:
added a "Brain fog summary" rule to `~/.claude/CLAUDE.md` (global, all
projects) — after any longer report/summary, append a short section: cut
parentheticals, one claim per line, bold only the scannable nouns. Applies
automatically going forward, not just when asked.

1 commit this session (`a6963e7`), not pushed — on top of `c7442ee` from
earlier in the same session (see previous handoff). Tree otherwise matches
the previous handoff's state (same 23-ahead count, same untracked
handoff-doc pile, now +1 for this doc and the previous one).

**Unresolved as of this handoff**: the full `pytest tests/unit/ -q` run
flagged in the previous handoff as "still running" **is still running**,
now ~41 minutes elapsed (started 11:17, this doc written 11:58). Checked
`/proc/<pid>/wchan` — it's in `hrtimer_nanosleep` (genuinely sleeping, not
deadlocked/hung on I/O), consistent with hitting real `time.sleep()` calls
somewhere in a retry/backoff test rather than being stuck — but 41+ minutes
for a suite documented as "no GPU or model download needed" is unusual
enough that a fresh session should verify rather than assume it's fine.
PID was 473788 as of this writing; check if it's still alive before
assuming anything about its outcome.

## Suggested skills

- **`read-handoff`** to pick up context (this doc).
- **`cheap-models-first`** before any subagent spawn — this session forked
  3 more parallel research agents for the new exchange docs (same pattern
  as the original 5), cheap since the session runs on Sonnet 5 and forks
  share its cache/context. Worth reusing for further exchange candidates
  the open thread below asks to keep finding.
- **`slice-workflow`** if landing the naive class gate (open thread carried
  over from before, still untouched) as a proper slice.

## What was done this session

*(This section covers only the work after `docs/handoff-202609061128.md`
was written — see that doc for the IBKR-recall/framework-build portion of
the same session.)*

1. **Saved a new standing communication preference, global not project.**
   Baz has ADHD and asked, after being shown a rewritten summary, to make
   "Brain fog summary" a standing rule appended after any longer report.
   Confirmed this belongs in `~/.claude/CLAUDE.md` (the user's private
   global instructions file, loaded for every project) rather than this
   project's memory system, since the memory system here is project-scoped
   and the ask was explicitly "general memory, not only project." Added a
   new top-level section there, next to the existing Cost policy / graphify
   sections.

2. **Researched 3 more broker/exchange candidates** via WebSearch (not yet
   forked at this point — direct research to scope what's worth a doc):
   Bitpanda, Coinmerce, OKX, Bitstamp (crypto) and Saxo Bank, Trading212,
   XTB (equities/CFD, via BrokerChooser-style "best Netherlands broker"
   searches). Presented a short table to Baz (strong: Saxo/OKX/Bitstamp;
   weaker/deprioritized: Bitpanda's fees are 1.00-1.49% vs. competitors'
   ~0.1-0.4%, Coinmerce's fees vaguely "higher end") and asked whether to
   write full docs for the 3 strong ones before committing to that much
   more forked work — confirmed yes.

3. **Wrote `docs/exchanges/README.md`** before spawning any new forks —
   an index/comparison table (initially with 3 rows marked "not yet
   checked") plus a 10-point checklist every exchange doc must satisfy
   (regulatory status; auth/account setup, specifically flagging the
   Finst/Alpaca-style "not actually self-service" trap; Tier 1 interface
   mapping table; the critical flat-floor-vs-percentage fee check; order
   size/precision; rate limits; sandbox/testnet availability; gotchas;
   explicit "not done this session" section; sources). Also documents the
   "when a finding changes the whole picture" pattern (put it in its own
   prominent section near the top, the way `finst.md`/`alpaca-europe.md`
   already do) and a short tone/rigor note (state uncertainty explicitly,
   no padding, "unknown" is an acceptable answer).

4. **Forked 3 parallel agents**, each pointed at the new README as the
   template plus `docs/broker-api-interface.md` for the interface itself,
   writing `docs/exchanges/{saxo,okx,bitstamp}.md`. All 3 returned clean,
   well-sourced, and consistent with the template on first pass — no
   rewrites needed, unlike a from-scratch review.

5. **Reviewed all 3 and synced two real findings back into the docs that
   made broader claims before these existed**:
   - `docs/broker-api-interface.md`'s "IBKR is the only Pattern A example
     so far" was wrong once Saxo's doc came back — Saxo's `precheck`
     endpoint is dry-run-order-based like IBKR's `whatIfOrder`, just
     single-call instead of needing `infer_commission_model`'s two-probe
     inference. Corrected, with Saxo now described as a "hybrid."
   - `roadmap/phase-5-auto-execution.md`'s equities section previously
     only flagged Alpaca Europe's B2B surprise; added Saxo alongside it as
     the more conventional-shaped option, with its own caveat (likely
     shares IBKR's flat-floor problem, unconfirmed).
   - `docs/exchanges/README.md`'s index table got its 3 placeholder rows
     filled in with real findings, plus a "cheapest to smoke-test" ranking
     line (Bybit EU / OKX / Bitstamp / Saxo all have a free demo/sandbox
     needing no funded account; Kraken/Bitvavo do not).
   - `CLAUDE.md`'s exchange-probe pointer section (added in the earlier
     part of this session) updated from "5 candidates" to "8", with the
     Saxo flat-floor risk folded into its existing Finst/Alpaca caveat
     list.
   - `project_broker_exchange_candidates.md` memory rewritten with the 3
     new findings, the cheapest-to-test ranking, and a note that
     `docs/exchanges/README.md` is now the more trustworthy/current source
     for the count and figures (memory can drift stale, the doc won't).

6. **Committed** (`a6963e7`) — 7 files, all the above.

## Published artifacts

None this session (no Artifact tool publishes) — all output is
repo-committed markdown, listed above.

## Git state

```
a6963e7 Document 3 more exchange candidates, add a docs/exchanges template
c7442ee Add a reusable broker/exchange cost-discovery probe framework
34adf28 Apply final currency-corrected margin figures from kairos-07's sweep
048acd7 Replace the measured figures with the currency-corrected full sweep
d219fae Make --report show costs per class and flag uncorrected margin rows
```

`git status -sb`: `master...origin/master/master [ahead 23]` — 2 new this
session's overall conversation (`c7442ee`, `a6963e7`), 21 pre-existing.
**Not pushed.** Remote quirk unchanged: the remote is *named*
`origin/master`, so `git log origin/master..HEAD` returns nothing — use
`git status -sb`.

Tree clean except the untracked `docs/handoff-*.md` pile (33 files now
including this one and `docs/handoff-202609061128.md` from earlier in this
same session — unpruned across many sessions, same standing housekeeping
item every recent handoff has noted).

No new test files this sub-session (docs-only changes) — nothing to run
beyond what the previous handoff already reported (`test_ibkr_instruments.py`
+ `test_exchange_probe.py`, 20 passed/7 skipped, verified earlier in the
session). The full-suite background run is still the open item — see TL;DR.

## Concluded threads

- **"Can we find more brokers?"** — yes, 3 more found and fully documented
  (Saxo, OKX, Bitstamp), same rigor as the original 5.
- **"Make a document outlining what exchange documentation should
  contain"** — `docs/exchanges/README.md`, done, and already load-bearing
  (the 3 forks used it directly).
- **Brain fog summary standing preference** — saved to global CLAUDE.md,
  active immediately (used in this same session's own responses after
  being asked).

## Open threads / possible next steps

**1. Keep finding more exchanges — explicit standing instruction, not a
   one-time ask.** Baz's words from earlier in this session: keep this in
   the open-threads list, and keep adding newly-found exchanges as more are
   discovered. No specific new names queued — this is "next time it comes
   up, or if asked, go find more" rather than a concrete unfinished search.

**2. Sign up for real accounts/sandbox keys and run a live probe against
   one of the 8.** Still not started (scaffolding + docs only has been the
   scope both times this came up). Cheapest with zero funded account, per
   `docs/exchanges/README.md`'s ranking: **Bybit EU, OKX, Bitstamp, Saxo**
   (all real free demo/sandbox — Saxo's is arguably the single best of any
   candidate: free, self-service, no KYC, €/$100k simulated balance).
   Kraken and Bitvavo need a real funded account first (no self-service
   sandbox found for either).

**3. Resolve Saxo's fee-figure disagreement specifically**, since it's the
   one open question with the highest stakes — if Saxo turns out to share
   IBKR's flat-floor problem, that's a second equities option ruled out for
   the same reason, not just an interesting caveat. `docs/exchanges/saxo.md`
   names the exact next step: a real `precheck` call against a SIM account
   for Kairos-sized ETF/equity orders.

**4. Full test suite result still unknown** — see TL;DR. Check whether PID
   473788 (or its successor if the shell session restarted) finished, and
   what it reported, before trusting "full suite green" for anything in
   this repo. If it's still running when this is picked up, that itself is
   worth investigating (41+ minutes is long for a suite documented as
   GPU-free) rather than continuing to wait indefinitely.

**5. The naive class gate — carried over from `docs/handoff-202609021247.md`
   via `docs/handoff-202609061128.md`, still not landed, still fully
   unblocked.** Add a `naive: bool = False` parameter to
   `resolve_disabled_strategies()` (`strategy/kairos_strategies.py`); when
   set, skip step 1 (the per-group oracle-tested check) entirely and make
   step 2's `stage` resolve to `"naive"` instead of `"finetuned"/"base"`.
   The trap flagged multiple times now: key the fall-through on the `naive`
   parameter itself, never on step 1 returning an empty set. Thread
   `naive=naive` from `kairos_signals._run_group`'s existing call site
   (`cefa274`).

**6. The `phantom_ledger` commission bug — carried over, still not fixed,
   still not decided who fixes it.** `CommissionModel.calculate()`'s
   `tiered` branch (`phantom/models/broker.py` in the sibling repo) reads
   keys `up_to`/`rate`; the real IBKR account profile uses
   `min_volume`/`max_volume`/`per_share`. Every papertrade run against the
   real account has been simulating €0.00 commission. Per the previous
   handoff: this is arguably lower-priority now that IBKR itself looks
   like the wrong venue regardless (see thread 7) — raise explicitly next
   session rather than silently deprioritizing.

**7. Whether IBKR is worth continuing to build against at all** — a
   conversation-starter, not a coded task, sharpened by this session: now
   there are 8 documented alternatives, several cheaper to even test
   (free sandboxes, no funded account) than continuing to work around
   IBKR's measured problems. Worth raising directly next session rather
   than defaulting back to IBKR-specific work.

**8. Inherited from earlier handoffs, still untouched**: small/mini model
   sweeps to match base's coverage; ~14 groups failing on the partial-bar
   NaN on the model path specifically; the stashed chunked-sampling VRAM
   prototype; wave packing committed but never run live; offline signal
   replay for selection/allocation tuning (deferred, not started); the
   long-dormant set (DEGIRO/BUX account signup, IB Gateway login,
   PREDCACHE-01, Epic E17, `n0`/`min_n` as global constants); housekeeping
   (33 unpruned handoff docs; `/tmp/papertrade_6m/`, `/tmp/naive_pt/`,
   `/tmp/pipe_wiring_test.db` — check whether these still exist before
   assuming they do, this note is now three sessions old).
