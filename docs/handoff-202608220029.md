# Handoff — 2026-08-22 00:29

## TL;DR

Continuing from `docs/handoff-202608212229.md` (read that one for the "where are the equities" investigation this segment resolves). Since then: **root-caused and fixed a real bug (BUG-10)** that silently killed every equity signal in 1h live signal generation, documented `CANDIDATE_UNIVERSE`'s scope and how to expand it (Baz asked why only 153 tickers exist when real markets have hundreds of thousands), investigated sourcing a broader universe from IBKR (blocked for now — no active gateway, and IBKR's site blocks automated fetches), and restarted the 3-month `signals_cache` sweep with the fix included (now ~74% done, 0 failures, crypto **and** equity signals both being generated this time). Nothing is blocked; this is a clean, steady-state monitoring situation.

## Suggested skills

None needed. Once the 3-month sweep finishes, the next step is exactly what was queued in the 22:29 handoff: precompute over the full window and check which of the 2-week-profitable combos stay winning — now with real equity data included for the first time.

## What was done this session (since the 22:29 handoff)

**Chased down the equity staleness bug precisely (BUG-10).** Root cause: `calendar_days_for_bars()` (`strategy/kairos_strategies.py`) already corrected for weekends (equities/FX/futures trade ~5/7 days — an earlier, already-shipped fix, see the test file's own docstring) but never corrected for equities *also* only trading ~6.5 hours/day (NYSE), not 24. `BARS_PER_DAY["1h"]=24` is crypto-calibrated; FX/futures trade near-continuously on weekdays so 24/day approximates them fine, but it undersized equities' calendar-day window by roughly 24/6.5x. Confirmed via a written report file's own `## Failures` section (never surfaced by my driver script, which calls `return_rows=False`): `"Not enough data for CB: need 300 bars, got 252"`.

Fixed by adding `is_limited_hours_equity_symbol()` and rescaling `bars_per_day` by `EQUITY_TRADING_HOURS_PER_DAY=6.5` for plain-ticker equities specifically, only at intraday granularity (daily-interval equities unaffected, FX/futures unaffected). 6 new regression tests in `tests/unit/test_fetch_data_raw_window.py`. Live-verified: all 8 previously-failing equities (CB, CRM, AAPL, ABBV, ABT, DE, COST, BA) now fetch 476 rows (vs the 300 needed) at the exact timestamp that failed before. Full suite green: 1696 passed. Committed `b4be6f9` (fix) + `f0b561c` (docs/todo.md BUG-10 entry).

**Important scope note**: this same `fetch_data_raw`/`calendar_days_for_bars` path is also used by the oracle/base/finetuned backtest stages (`kairos_strategies.py:1087`), not just live signal generation — so this bug likely also explains why only 8 of the 46 equities with 1h oracle data ever got a matching base run (the rest may have failed the same "not enough data" way during their own base backtest attempts, historically). Re-running the discovery sweep for equities post-fix should recover meaningfully more coverage than any pre-fix attempt.

**Documented `CANDIDATE_UNIVERSE`'s scope and how to change it** (Baz asked why only 153 tickers exist when real markets have hundreds of thousands of stocks/ETFs/futures/indices/mutual funds/FX pairs). Added a full new subsection to `strategy/PIPELINE.md` (right before "## 2. Storage") covering: it's a hand-typed dict with no external source, why it's deliberately small (indices/mutual funds aren't appropriate inputs regardless of size; GPU cost scales per-survivor, not per-candidate — universe screening itself is cheap and does the real narrowing), exactly how to edit it, and that there's no IBKR API integration in this repo yet (only a static margin-rate config, `config/margin_ibkr.yaml`, unrelated to instrument discovery). Committed `840be4f`.

**Investigated sourcing a broader universe from IBKR specifically** (Baz's stated preference: the universe should reflect what's actually tradeable on IBKR). Two paths exist: live TWS/Gateway API query, or IBKR's own downloadable per-exchange product listings. Baz confirmed the gateway isn't active right now and can't be activated currently. Tried the static-listings path anyway via `WebSearch`/`WebFetch` — found IBKR's "Product and Exchange Listings" page (`interactivebrokers.com/en/pricing/product-exchange-overview.php`) but it returned `403 Forbidden` to automated fetch (bot-blocked, unsurprising for a broker site). **Concluded this is blocked for now on both fronts** — no code changes attempted, the docs already capture both paths for whenever the gateway is available or a manual export becomes possible.

**Restarted the 3-month `signals_cache` sweep with the equity fix included.** The version that had been running since 22:10 (from the previous handoff) started *before* the fix landed and would have had the exact same equity-blindness problem — killed it at ~22% progress (no orphaned processes, clean stop) and relaunched fresh at 22:46:33 so the whole window gets equity coverage too, not just crypto. As of this handoff: **1360/1843 hourly steps done (~74%), 0 failures**, steady ~2.5-3s/step pace. Should finish within the next 30-60 minutes.

## Published artifacts

None new. `kairos_1h_combo_stats.xlsx` (sent via `SendUserFile` in an earlier segment) remains the only file delivered this session.

## Git state

Branch `master`, HEAD `840be4f`. New commits this segment:
```
b4be6f9 Fix equity 1h data-window undersizing (BUG-10): correct for trading-hours, not just weekends
f0b561c Document BUG-10 (equity trading-hours undersizing) in docs/todo.md
840be4f Document CANDIDATE_UNIVERSE's scope and how to expand it
```
(on top of `68c10db` and earlier from prior segments). Working tree clean except accumulating untracked `docs/handoff-*.md` files (now 10 including this one — strongly worth pruning once Baz has reviewed the whole session).

## Concluded threads

- BUG-10 (equity trading-hours undersizing) — fully root-caused, fixed, tested, committed.
- `CANDIDATE_UNIVERSE` scope/expansion documentation — written and committed.
- The IBKR-sourcing investigation — concluded (blocked for now on both the live-API and static-listing fronts), not abandoned — the docs point the way for whenever it's unblocked.

## Open threads / possible next steps

**1. The 3-month `signals_cache` sweep is running — check on it first.** ~74% done as of this handoff (1360/1843 steps, 0 failures), started fresh at 22:46:33 with the equity fix included. Check via `tail -f data/signals_cache_populate_1h.log` or `ps aux | grep populate_signals_cache_1h`. Once complete:
   - Re-run `--precompute` over the full window: `uv run ./strategy/kairos_signal_replay.py --precompute --start 2026-05-23 --end 2026-08-21 --interval-ladder 1h`.
   - Pull the same per-(strategy,ticker,model) breakdown query used in the 20:29/22:29 handoffs, now over the full 3 months, to see which of the 2-week-profitable combos (per the 22:29 handoff's combo tables / the `kairos_1h_combo_stats.xlsx` spreadsheet already sent) stay winning over the longer window — this is the concrete analytical step Baz asked for, still not done because the data wasn't ready until now.
   - This 3-month run should, for the first time, include real equity signal data (CRM, ABBV, CB, AAPL) — worth specifically checking whether they show up now, as a live confirmation the BUG-10 fix actually worked end-to-end in the full sweep context, not just in the isolated `fetch_data_raw` test.

**2. Consider re-running `--stage auto`/`--stage base` for equities now that BUG-10 is fixed.** Per the analysis above, this bug likely blocked many of the 46 oracle-tested-but-not-base-matched equities from ever getting a proper base run. Re-running `--stage base` (or the fuller `--stage auto`) for the `equity` asset class could unlock meaningfully more finetune-eligible candidates than the 8 that existed before today. Not started — a natural next step once the sweep/replay analysis above is done, if Baz wants to expand past today's crypto-heavy batch.

**3. JTO-USD still needs a manual re-queue** (unchanged, low priority, several handoffs old) — `--stage finetune_next --assets JTO-USD --interval 1h --backtest_period 3m --ft_epochs 30 --ft_batch_size 32 --skip-idle-check`.

**4. IBKR-sourced universe expansion — blocked, revisit when the gateway is available**, or if Baz can manually export a product listing from a logged-in browser session and hand it over as a file.

**5. Everything else from earlier handoffs is unchanged**: PREDCACHE-01 ticket unimplemented, `viability_report`/`finetuned_models` registry drift (ZW=F affected) not investigated further, Epic E17 not started, standing terminology note (Baz is 44, prefers 1990s-2000s computing terminology).

**6. Housekeeping**: 10 handoff docs accumulated. Prune all but this one once reviewed.
