# Handoff — 2026-08-22 02:38

## TL;DR

Continuing from `docs/handoff-202608220029.md` (read that one for the BUG-10 equity fix and `CANDIDATE_UNIVERSE` documentation — this doc is the payoff of that work). The 3-month `signals_cache` sweep finished clean (1844/1844 steps, 0 failures, equity signals confirmed present for the first time), precompute ran over the full window, and the actual persistence check Baz asked for is **done and delivered**: of 15 combos that looked profitable in a 2-week window, only **4 actually held up over 3 months** — and those 4 got *more* profitable with more data, not less, which is the pattern you want to see. A comparison spreadsheet was sent. No code changes this segment; this was pure analysis on top of already-fixed infrastructure. Nothing is currently running in the background — the monitoring loop was deliberately stopped since the ball is in Baz's court on the next step.

## Suggested skills

None needed. If Baz asks for the 4-combo-restricted capital-weighted replay next, that's more `kairos_signal_replay.py`/SQL work, no special skill.

## What was done this session (since the 00:29 handoff)

**The 3-month sweep finished clean**: 1844/1844 hourly steps, 0 failures (log: `data/signals_cache_populate_1h.log`). Confirmed equity signals now genuinely exist — `signals_cache` has thousands of rows each for AAPL, ABBV, CB, CRM, none of which existed before the BUG-10 fix. This is real, live, end-to-end proof the fix from the previous segment worked, not just the isolated `fetch_data_raw` unit test.

**Ran `--precompute` over the full 3-month window** (`2026-05-23` to `2026-08-21`): 31,221 signals unpacked and closures computed, 0 errors.

**Ran the actual capital-weighted `--replay`** over the same window (`--interval 1h --capital 200 --max-pos-pct 15 --top-k 3`, matching the 2-week check's parameters exactly for comparability): **-16.34 EUR (-8.17%), 2082 trades, 17.4% max drawdown.** Still a net loser overall on the naive top-3-everything allocation — consistent with the 2-week result, confirming raw/unfiltered signals losing money isn't a fluke of the shorter window.

**Did the actual persistence check** (the concrete analytical step queued since the 20:29 handoff): pulled the same per-`(strategy, ticker, model)` breakdown over the full 3-month window and cross-referenced against the 15 combos that were profitable in the 2-week check. Result:

| Combo | 2-week sum% | 3-month sum% | Verdict |
|---|---|---|---|
| path_high_low_execution / ZEC-USD / Finetuned | +19.21 | **+260.82** | Durable, strengthened |
| path_high_low_execution / ZEC-USD / Base | +16.63 | **+117.17** | Durable, strengthened |
| path_v_shape / ZEC-USD / Finetuned | +9.62 | **+173.10** | Durable, strengthened |
| path_execution / XLM-USD / Base | +10.26 | **+85.04** | Durable, strengthened |
| *(11 others, including the two biggest 2-week standouts)* | up to +59.57 | flipped negative (as low as -220) | Not durable |

**4 of 15 combos persisted — and all 4 got stronger with more data, not weaker**, which is exactly the pattern that distinguishes real edge from a lucky short window. The other 11 (including `high_low`/ZEC-USD/Finetuned at +59.57 and `path_high_low_execution`/JTO-USD/Base at +42.05 — the two biggest apparent winners in the original 2-week report) did not hold up.

Built and sent a comparison spreadsheet: `kairos_1h_persistence_check.xlsx` (85 total combo rows, 2-week and 3-month columns side by side, color-coded: green=durable/improved, red=flipped-negative/still-losing, yellow=only-appeared-in-one-window).

**Offered the natural next step** — simulating a capital-weighted replay restricted to just the 4 durable combos, rather than the naive top-3-across-everything allocation, to see what real, curated performance would look like. **Not yet done — Baz hasn't responded to this offer.**

## Published artifacts

Two files delivered via `SendUserFile` this session (not hosted Artifacts — local file transfers):
- `kairos_1h_combo_stats.xlsx` (2-week combo stats, sent in an earlier segment — see the 22:29 handoff)
- `kairos_1h_persistence_check.xlsx` (2-week vs 3-month comparison, sent this segment)

## Git state

Branch `master`, HEAD `840be4f` — unchanged since the 00:29 handoff, no commits this segment (pure analysis on top of already-committed infrastructure). Working tree has the same accumulating untracked handoff docs (now 11 including this one) — genuinely worth pruning once Baz has reviewed the whole session end to end.

## Concluded threads

- The 3-month `signals_cache` sweep — completed clean, equity coverage confirmed.
- The precompute + replay over the full window — done.
- **The persistence check itself — done and delivered.** This was the concrete deliverable queued since the 20:29 handoff and is now answered: a narrow but credible 4-combo set survives, everything else in the earlier "profitable" list was noise.

## Open threads / possible next steps

**1. Immediate: build the 4-combo-restricted replay, if Baz wants it.** Offered, not yet actioned. Given `--signal-selection`'s DSL can't express an exact-whitelist filter (established earlier this session — no `IN`, no OR-across-conditions, no Model column), this would need direct SQL filtering of the precomputed `papertrade_signals`/`papertrade_signals_closure` tables down to just the 4 winning `(strategy_name, ticker, model_label)` triples before replaying, or a small standalone script reusing `BacktestEngine`'s allocation logic restricted to that subset. The exact 4 triples are: `(path_high_low_execution, ZEC-USD, Finetuned(ZEC-USD))`, `(path_high_low_execution, ZEC-USD, Base)`, `(path_v_shape, ZEC-USD, Finetuned(ZEC-USD))`, `(path_execution, XLM-USD, Base)`.

**2. Consider re-running `--stage auto`/`--stage base` for equities now that BUG-10 is fixed** (unchanged from the 00:29 handoff) — likely unlocks meaningfully more of the 46-oracle-tested-but-not-base-matched equities.

**3. JTO-USD still needs a manual re-queue** (unchanged, very low priority now, many handoffs old).

**4. IBKR-sourced universe expansion — blocked** (gateway inactive, IBKR's site blocks automated fetches). Revisit when the gateway is available or a manual browser export becomes possible. Docs already capture both paths in `strategy/PIPELINE.md`.

**5. Everything else from earlier handoffs unchanged**: PREDCACHE-01 ticket unimplemented, `viability_report`/`finetuned_models` registry drift (ZW=F affected) not investigated further, Epic E17 not started, standing terminology note (Baz is 44, prefers 1990s-2000s computing terminology).

**6. Housekeeping**: 11 handoff docs accumulated across this session. Strongly worth pruning all but this one now that the persistence-check arc (the main thread of the whole day) has actually concluded with a real answer.
