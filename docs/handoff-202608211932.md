# Handoff — 2026-08-21 19:32

## TL;DR

This is the final handoff for a long, single-session arc that ran from ~09:00 to ~19:30 today. Seven handoff docs accumulated (`docs/handoff-20260821{0919,1029,1229,1429,1629,1829,1932}.md`) — **this one supersedes all of them; read only this one to resume.** Summary of the whole day: fixed a real $vol=0.0 crypto liquidity bug, ran a full catch-up batch of 1h finetune candidates (10 accepted, 6 rejected, 1 self-inflicted failure), fixed two more real bugs found along the way (a hardcoded notify-gating bug and a systemic test-hygiene gap causing real Telegram sends from unit tests), reclaimed ~18GB of disk from never-deleted rejected checkpoints, and — most importantly — **ran an actual out-of-window persistence check on today's batch results, which lost money** (-3.91% over a 2-week replay, concentrated in the second week). This directly contradicts the batch's own impressive-looking accept-gate Sharpe numbers and is the central open question going into the next session: **is today's finetune batch actually good, or was it overfitting?** All work is committed; nothing is running in the background; the monitoring loop has been stopped. This is a clean stopping point, not a mid-task interruption.

## Suggested skills

- None required to pick this back up — it's plain investigation/decision-making, not implementation-ready work.
- If the next session decides to slice the replay loss by individual model/asset (the natural next diagnostic step, offered but not yet done), no special skill needed — just more `kairos_signal_replay.py` invocations against the existing precomputed closures.
- If the next session decides PREDCACHE-01 is worth doing, that's a normal `/breakdown`-then-`/run-stories`-shaped ticket already written and ready.

## What was done this session

**Morning (~09:00–10:30):** Fixed BUG-05, a real $vol=0.0 bug in 1h universe screening — two stacked issues in `compute_universe_stats` (sparse yfinance crypto volume reporting, and crypto's `volume` column being already dollar-denominated rather than share-denominated). Commits `20bfd0e`, `281c3b2`. Full detail in git log / `docs/todo.md`'s BUG-05 entry — not repeated here.

**Late morning: the finetune catch-up decision.** Baz asked to revisit 1h finetuning before committing to a full discovery sweep, suspecting bugs had caused skipped work. Investigation confirmed a real bug (BUG-07: `select_finetune_candidate` had no interval filter, so auto-select always preferred 1d over 1h) plus 3 pre-existing failed candidates likely killed by an already-fixed DST bug. Fixed BUG-07 (commit `a922a76`). Built a throwaway batch script (`/tmp/.../scratchpad/run_finetune_batch_1h.py` — session-scratchpad path, may not survive; its logic and full history of iteration is in this conversation's transcript, not worth reconstructing from scratch if gone since the batch is now done) to retry the 3 failed candidates plus auto-select 15 more, with epochs scaled to each candidate's data size (calibrated against real timing measurements: floor 10 epochs, cap 30, targeting ~25min per cycle).

**Afternoon: running the batch (~7.5 hours wall-clock, 11:17–18:44).** Hit and fixed two operational bugs along the way (GPU-busy skips being mis-treated as completed cycles; an overly-strict GPU-idle threshold fighting this machine's baseline utilization) — both fixed in the batch script itself, not the codebase. One candidate (JTO-USD) is a real casualty: it trained successfully but crashed on a broken pipe when an earlier `pkill` (cleaning up the first, buggy batch-script launch) orphaned its process and severed its stdout — it's stuck at `status='failed'` with a real, unevaluated checkpoint; **still needs a manual re-queue** (see Open threads).

**Final batch tally (1h finetuned_models registry):** 10 accepted total (2 pre-existing: ZEC-USD, ZW=F; 8 new today: HBAR-USD, CRM, ABBV, CB, AAPL, XLM-USD, UNI7083-USD, SOL-USD), 6 rejected (COST, BA, DE, ABT, BCH-USD, DASH-USD), 1 failed (JTO-USD).

**Along the way, two more real bugs found and fixed** (both while Baz was asking about test hygiene / a subagent fix for one failing test):
- **BUG-06**: a hardcoded `enabled=True` on one `_notify()` call in `run_stage_finetune_next`, bypassing `--no-telegram`. Fixed via a dispatched Haiku subagent. Commit `385a7eb`.
- **BUG-09**: BUG-06 alone didn't fully solve Baz's real complaint ("I still get Telegram messages from tests") — root cause was a separate, systemic test-hygiene gap: several tests called real production notification code paths (`kairos_gpu.ensure_cuda()`, `kairos_papertrade.prewarm_prediction_cache()`) without mocking `send_telegram` at all. Fixed with a global `autouse` fixture in `tests/conftest.py` that no-ops `send_telegram`/`send_telegram_document` in every module that imports either, for every test, by default. Commit `68c10db`.

**BUG-08**: rejected finetuned checkpoints were never actually deleted from disk (only an empty `REJECTED` marker was written) — found 23 accumulated rejected checkpoints (18 pre-existing at 1d, 5 from today) wasting ~18GB. Fixed in `run_stage_finetune_next`'s reject branch; existing 23 cleaned up retroactively. Commit `ce9c610` (bundled with BUG-06/07 retroactive `docs/todo.md` documentation).

**Wrote `docs/tickets/PREDCACHE-01-finetune-verification-persistent-cache.md`** — a not-yet-implemented story for caching finetune-verification predictions the same way `kairos_papertrade.py` does, plus purging a rejected model's cached predictions. Written in response to Baz's precise question about whether verification predictions are cached (answer: no, confirmed by tracing the code).

**Evening: the persistence check (the most important part).** Baz's own instruction, executed once the batch finished: ran `kairos_signals.run()` across an hourly cadence over the last 2 weeks to populate `signals_cache` for all the newly-accepted models, then `kairos_signal_replay.py --precompute` + `--replay` on top of that. Hit and fixed a real efficiency bug along the way (my first driver never set `KAIROS_PRED_CACHE_DIR`, so every call reloaded every model from scratch — 8+ minutes per call; fixed by reusing `kairos_papertrade.prewarm_prediction_cache()` to warm the cache model-major first, bringing per-call time down to ~1.5-2s). Baz also caught and correctly diagnosed a false alarm along the way (some models appeared to get skipped in the prewarm log — turned out to be a truncated-log artifact, not a real bug, confirmed against the full untruncated log).

**The actual result: `--replay --interval 1h --start 2026-08-07 --end 2026-08-21 --capital 200 --max-pos-pct 15 --top-k 3` lost money.** Full window: -7.83 EUR (-3.91%), 55 trades, 4.07% max drawdown. Week 1 (Aug 7-14): nearly flat, +0.16%, only 6 trades. Week 2 (Aug 14-21): -4.07%, 49 trades — essentially all the activity and all the damage. This directly contradicts the accept-gate's own Sharpe numbers (many of today's accepted models showed finetuned mean_sharpe of 12-30, vs base's 2-7) — a gap partially explained by legitimate 1h vs 1d annualization-factor scaling (~4.9x) and partially by the accept-gate's `mean_sharpe` metric itself averaging only pre-filtered "viable" (already-positive) strategies, but the net replay result suggests the accept gate's numbers are not translating into real, persistent edge. **This was reported to Baz but not yet resolved or acted on** — it's the central open question for next time.

## Published artifacts

None from today. The one artifact referenced across the whole session is a papertrade report from *before* today (`https://claude.ai/code/artifact/d67076b5-a260-422f-b7f4-5dba1b56d3e6`), unrelated to today's work.

## Git state

Branch `master`, HEAD `68c10db`. Today's commits, oldest to newest:
```
20bfd0e Fix universe $vol=0.0 bug: resample-sum sparse volume + crypto dollar-unit fix
281c3b2 Update docs to reflect BUG-05 ($vol=0.0) fix landing
a922a76 Fix select_finetune_candidate ignoring interval filter in auto-select
385a7eb Fix notify-gating bug: change enabled=True to enabled=notify in backtesting-finetuned notification
ce9c610 Delete rejected finetuned checkpoints' weights from disk; document BUG-06/07/08
68c10db Add global test safety net against real Telegram sends (BUG-09)
```
Working tree clean except 7 untracked `docs/handoff-*.md` files from today (this one is the 7th) — genuinely worth pruning the older 6 once reviewed, they're all superseded by this one. Nothing pushed — per standing preference, commit freely, push only when explicitly asked.

## Concluded threads

- BUG-05, BUG-06, BUG-07, BUG-08, BUG-09 — all fixed, tested, committed. See `docs/todo.md`'s "Bugs found via live verification" section for the authoritative per-bug detail (not repeated here).
- The 1h finetune catch-up batch itself — fully run to completion (17 total cycles across the whole registry, batch stopped itself cleanly once `select_finetune_candidate` had no more qualifying candidates).
- The "does finetune verification cache predictions" question — answered (no), and turned into ticket PREDCACHE-01.
- The 18GB disk-space cleanup — done.
- The persistence-check *mechanism* (populate signals_cache → precompute → replay) — built, debugged (prewarm efficiency fix), and run successfully. The *mechanism* is concluded; the *result* it produced is not (see below).

## Open threads / possible next steps

**1. The central open question: is today's finetune batch actually good?** The 2-week replay lost money (-3.91%) despite the accept-gate's Sharpe numbers looking very strong for several models. This needs a decision from Baz on how to proceed — possibilities include: slicing the replay by individual model/asset to see if the loss is concentrated in a few bad models rather than spread across all 10 (offered, not yet done); reconsidering whether `min_signals=3` and the current accept-gate criteria (`ft_count > base_count` or tied-with-higher-mean) are too permissive for 1h; or treating this as evidence the whole approach needs recalibration before trusting any 1h finetuned model in production. **Do not proceed to the discovery sweep or trust today's accepted models in live trading until this is resolved.**

**2. JTO-USD still needs a manual re-queue.** Registry row id=178, `status='failed'` from the broken-pipe accident, not a real training problem. Re-queue via `--stage finetune_next --assets JTO-USD --interval 1h --backtest_period 3m --ft_epochs 30 --ft_batch_size 32 --skip-idle-check` whenever convenient — low priority now given the bigger open question above.

**3. PREDCACHE-01 ticket is written but unimplemented.** Not urgent.

**4. `viability_report` and `finetuned_models` are two independent registries that can drift out of sync** — discovered while investigating the prewarm sweep: ZW=F is an accepted 1h finetuned model but has zero rows in `viability_report` (the table `kairos_signals.py`'s `load_work_items()` reads from, populated only by `--stage auto`), so it's invisible to `kairos_signals.run()`/live trading entirely despite being "accepted." Worth a ticket if this turns out to affect more models than just ZW=F — not investigated further this session.

**5. Epic E17 (hourly Telegram digest + scheduling) — still not started.** Unchanged from every previous handoff today.

**6. The actual 1h discovery sweep — still not started, deliberately deferred**, and now additionally gated on resolving item 1 above (if today's batch's numbers don't hold up under replay, that's a strong signal the sweep's own oracle/base Sharpe numbers would need the same skepticism before trusting them either).

**7. Standing terminology note**: Baz is 44, prefers 1990s-2000s computing terminology (e.g. "driver" = peripheral-interface library, not an orchestration script) — saved to memory `user_age_and_terminology.md`, apply in future sessions.

**8. Housekeeping**: consider deleting the 6 earlier `docs/handoff-2026082*.md` files from today now that this one supersedes them, next time someone's in the repo with Baz's OK.
