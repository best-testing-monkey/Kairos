# Handoff — 2026-09-13 16:23

## TL;DR

Short session. Continued from `docs/handoff-202609061646.md`. Two things
happened: (1) confirmed via Gmail (Gmail MCP) that Bybit EU's ID
verification cleared — same day as signup, 2026-09-06 23:32 UTC, much
faster than the "up to 3 days" estimate; (2) Baz then provided real
(non-testnet) Bybit EU API credentials with read+write scope, which were
live-validated the same way OKX/Saxo/Bitstamp were — **Bybit EU is now
the fourth exchange confirmed viable**, but with the narrowest crypto
universe coverage of the three crypto exchanges tested (66% vs. OKX's 87%
and Bitstamp's 79%).

**Headline results:**
- **Bybit EU ID verification confirmed cleared via Gmail search** — email
  subject "[Bybit-EU]Identiteitsverificatie (standaard) is goedgekeurd",
  received 2026-09-06 23:32 UTC.
- **Bybit EU LIVE-TESTED AND CONFIRMED VIABLE**, real account (not
  testnet). Baz asked for a **read+write** key (Trade permissions on
  Unified Trading, SPOT, Convert) since live execution is a later goal;
  was told/confirmed **system-generated HMAC** is the right key type over
  self-generated RSA (HMAC is what ccxt/plain REST signing expects; RSA
  means implementing your own signing pipeline for no benefit on a
  discovery probe). Only read-only calls were made despite the write
  scope — no order or convert was ever placed.
- **Fees confirmed live: 0.10% maker / 0.25% taker** — corrects
  `docs/exchanges/bybit-eu.md`'s pre-validation guess of a flat
  0.10%/0.10% (was wrong on the taker side, same maker/taker asymmetry
  every other crypto exchange here shows). No flat floor. €1 EUR minimum
  order (BTCEUR).
- **Environment-locking confirmed, and it's a genuine two-venue split**:
  the EU-issued key authenticates on `api.bybit.eu` and is flatly rejected
  (`retCode 10003, "API key is invalid"`) on `api.bybit.com` — and the two
  domains showed different live BTCEUR prices at the same instant
  (66,497.6 vs. 66,328.9), confirming separate liquidity pools, not just
  routing. Resolves the doc's previously-untested ccxt-endpoint-override
  question.
- **Bybit EU universe coverage measured: 41/62 (66%)** — narrowest of the
  three crypto exchanges probed. 12 direct EUR pairs; 29 more via a
  genuine `USDC/EUR` pair (indirect-route rule from
  `feedback_exchange_coverage_indirect_routes.md` applied **from the
  start** this time, not as a correction afterward like Bitstamp's ZEC
  case). 21 symbols genuinely absent. 15 new candidates clear the volume
  floor (5 overlap OKX's own candidate list, 10 are net-new).

**Credentials handling** (same discipline as OKX/Saxo/Bitstamp): real API
key + secret pasted into the conversation by Baz, used only as env vars to
a throwaway scratchpad script (`/tmp/.../scratchpad/bybit_probe.py` and
`bybit_coverage.py` — outside the repo, session-scoped). Grepped the repo
for both raw values before each commit — clean both times. Never written
to any repo file.

2 commits this session (`8bf83e1`, `8bb5bb2`), both on `master`, **none
pushed**.

## Suggested skills

- **`read-handoff`** to pick up context (this doc + `docs/handoff-202609061646.md`
  for the fuller exchange-probe arc, and `docs/handoff-202609061158.md` for
  the original OKX/Saxo work if needed).
- **`cheap-models-first`** before any subagent spawn.
- If the leverage-regression test file (open thread carried over twice now)
  gets picked up, no special skill — just re-run it directly.

## What was done this session

1. **Checked Gmail (Gmail MCP `search_threads`) for Bybit EU ID
   verification confirmation**, per Baz's request. Found it: "Beste
   trader, Je identiteitsverificatie (standaard) is goedgekeurd" (Dutch —
   "identity verification (standard) approved"), 2026-09-06 23:32 UTC —
   same day as the account signup logged in the prior handoff, not the
   "up to 3 days" originally estimated.

2. **Answered Baz's question on which Bybit API key type to generate.**
   Recommended system-generated (HMAC) over self-generated (RSA) — HMAC
   is what `ccxt` and every plain-REST signing example (including the
   native `/v5/account/fee-rate` call `docs/exchanges/bybit-eu.md`
   documents) expect out of the box; RSA requires generating and managing
   a keypair locally and implementing RSA-SHA256 signing yourself, real
   work for a throwaway discovery probe.

3. **Baz provided real Bybit EU credentials** (read+write scope: Unified
   Trading Trade, SPOT Trade, Convert — deliberately not read-only, since
   Baz wants live execution later). Wrote a throwaway scratchpad script
   (`bybit_probe.py`) using plain HMAC-SHA256 request signing (no ccxt —
   native REST, matching the pattern already used for Bitstamp), env vars
   only. Ran 6 real calls: public ticker on both `api.bybit.eu` and
   `api.bybit.com`, authenticated `wallet-balance` on both (auth-scope
   check), authenticated `fee-rate` on both, public `instruments-info`
   (131 spot symbols on the EU domain), and precision/limits for BTCEUR.
   No order or convert call was made — the probe never exercises the
   write scope, matching the discovery-only pattern used for every prior
   exchange even when the key had trade permissions.

4. **Grepped the repo for both raw credential values before any commit**
   — clean.

5. **Universe coverage measured** (second scratchpad script,
   `bybit_coverage.py`) against Kairos's 62-symbol crypto universe
   (`kairos_pipeline.CANDIDATE_UNIVERSE["crypto"]`), applying the
   indirect-conversion-route rule (`feedback_exchange_coverage_indirect_routes.md`)
   from the start rather than as an afterthought: confirmed `USDC/EUR` is
   itself a real Bybit EU spot pair, so USDC-quoted-only bases count as
   covered. Result: 41/62 (66%). Then checked all non-universe
   EUR/USDC-quoted bases against the same ~8,139 EUR/24h volume floor used
   for OKX/Bitstamp, found 16 candidates clearing it, excluded `EURC`
   (stablecoin) → 15 real candidates, cross-referenced against OKX's own
   38-candidate list to correctly flag 5 overlaps (`PUMP`, `VIRTUAL`,
   `TRUMP`, `PENGU`, `XPL` — all more liquid on OKX) rather than assuming
   no overlap.

6. **Updated `docs/exchanges/bybit-eu.md`**: corrected the fee section
   (0.10%/0.25%, not flat 0.10%/0.10%), confirmed order size/precision for
   BTCEUR, resolved the ccxt-EU-endpoint gotcha as tested-and-confirmed
   (including the genuine two-venue-price evidence), and rewrote "What's
   needed before this can be probed for real" as done. Committed `8bf83e1`.

7. **Updated `docs/universe-expansion-candidates.md`**, replacing the
   "Bybit EU — pending" stub with the full coverage writeup (direct/
   indirect/absent breakdown, new-candidate table with OKX-overlap
   annotations). Committed `8bb5bb2`.

8. **Updated `docs/exchanges/README.md`**: index-table row for Bybit EU
   (confirmed fee, confirmed env-lock), a new "fourth candidate
   live-tested" paragraph mirroring the existing Saxo/OKX/Bitstamp
   paragraphs, and corrected the "real signup latency" bullet for Bybit EU
   (same-day, not the originally-reported up-to-3-days). Folded into the
   same two commits above (with the corresponding doc each belongs to).

9. **Memory updated**: `project_broker_exchange_candidates.md` (new Bybit
   EU section: live-validation findings, universe coverage, credentials
   handling; frontmatter description bumped to "4 live-tested"), and the
   `MEMORY.md` index line for that memory.

## Published artifacts

None this session (no Artifact tool publishes) — all output is
repo-committed markdown plus memory files.

## Git state

```
8bb5bb2 Measure Bybit EU's crypto universe coverage against Kairos's 62 symbols
8bf83e1 Confirm Bybit EU fees and env-locking with a live test
```

(Both stacked on top of `f307427`, the tip from the prior handoff.)

`git status -sb`: `master...origin/master/master [ahead 38]`. **Not
pushed.** Remote quirk unchanged: the remote is *named* `origin/master`,
so `git log origin/master..HEAD` returns nothing — use `git status -sb`.

Tree clean except the untracked `docs/handoff-*.md` pile (still unpruned,
now 35 untracked per `git status` at session start, plus this new one —
same standing housekeeping item every recent handoff has noted).

No test suite was run this session.

## Concluded threads

- **Bybit EU ID verification status** (prior handoff's open thread #2,
  first half) — confirmed cleared via Gmail, same-day not multi-day.
- **Bybit EU live-validation pattern** (prior handoff's open thread #2,
  second half) — done: fees, environment-locking, order size all
  confirmed live, matching the OKX/Saxo/Bitstamp treatment.
- **Bybit EU universe coverage** — measured and documented, applying the
  indirect-routes rule from the start as instructed in the prior handoff.

## Open threads / possible next steps

**1. Re-run `test_kairos_papertrade_leverage_regression.py`.** Carried
   over twice now (from `docs/handoff-202609061513.md` and
   `docs/handoff-202609061646.md`) — diagnosed as GPU-lock contention with
   a sibling session's `scripts/kairos_weekly_discovery.py` (PID 427566),
   which was confirmed gone as of this session's start. Should just pass;
   still not actually re-run.

**2. Formalize OKX/Bitstamp/Bybit EU into the real `ExchangeProbe`
   framework** (`scripts/{okx,bitstamp,bybit_eu}_instruments.py`, per
   `docs/playbooks/add-exchange-probe.md`). Still not started — three
   exchanges are now validated, all with ad-hoc throwaway scripts rather
   than the reusable Tier-1 abstraction.

**3. Decide on an actual execution venue among OKX/Bitstamp/Bybit EU.**
   All three are now live-tested and viable on fees (no flat floor). The
   tradeoff is now concrete: OKX has the best universe coverage (87%) but
   needed a gotcha fix (`eea.okx.com`); Bitstamp is middle (79%); Bybit EU
   is narrowest (66%) but is the only one with a real spot-margin/leverage
   story (see `docs/exchanges/bybit-eu.md`'s margin section) if leverage
   ever matters. This decision has never explicitly been asked for or
   made — worth surfacing directly rather than continuing to add more
   exchanges.

**4. Run the deduped-group re-screening pipeline against
   `docs/universe-expansion-candidates.md`'s candidates** (38 from OKX + 4
   from Bitstamp + 15 from Bybit EU, with real cross-exchange overlaps now
   documented) — per `project_crypto_commodity_universe_expansion` memory's
   stated order: universe screening → correlation/grouping → oracle +
   naive → base → small/mini → regenerate `docs/papers/`. **TRX (TRON)**
   still flagged as worth checking first — top-15 market-cap coin missing
   from the universe for no identified reason.

**5. Whether IBKR is worth continuing to build against at all** — now
   even more pointed with three crypto exchanges confirmed cheaper with no
   flat floor. Still just a conversation-starter, not a coded task.

**6. The `phantom_ledger` commission bug** — carried over, still unfixed,
   lower urgency given IBKR itself looks like the wrong venue regardless.

**7. The naive class gate** — carried over from
   `docs/handoff-202609021247.md`, still not landed, still fully
   unblocked.

**8. Inherited from earlier handoffs, still untouched**: small/mini model
   sweeps to match base's coverage; ~14 groups failing on the partial-bar
   NaN on the model path specifically; the stashed chunked-sampling VRAM
   prototype; wave packing committed but never run live; offline signal
   replay for selection/allocation tuning (deferred, not started); the
   long-dormant set (DEGIRO/BUX account signup, IB Gateway login,
   PREDCACHE-01, Epic E17, `n0`/`min_n` as global constants); housekeeping
   (35+ handoff docs now, most unpruned — sixth+ session running on this
   note; `/tmp/papertrade_6m/`, `/tmp/naive_pt/`, `/tmp/pipe_wiring_test.db`
   — check whether these still exist before assuming they do).
