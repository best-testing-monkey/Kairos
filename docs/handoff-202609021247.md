# Handoff — 2026-09-02 12:47

## TL;DR

Continues from `docs/handoff-202609011511.md` (papertrade thread, handed over
mid-session by the sibling "fix naive" session). That session's sweep finished,
its DB-write embargo lifted, and its 8 orphaned pool workers were killed by the
sibling itself — that whole thread is now closed. This session's own focus
shifted to three things: **wiring `--stage naive` into the pipeline CLI**,
**a real 6-month base papertrade run**, and **a documentation terminology
cleanup** (the word "corpus" was doing two contradictory jobs in CLAUDE.md).

**The single most load-bearing finding this session: `phantom_ledger`'s
commission model is broken for the account Kairos actually trades through.**
`CommissionModel.calculate()`'s `tiered` branch reads keys `up_to`/`rate`; the
account's real `IBKR` broker profile's tiers are keyed `min_volume`/
`max_volume`/`per_share`. The mismatch means `tier_rate` silently resolves to
`0.0` on every trade, and the tiered branch never applies the profile's own
`$1` `per_share_min` floor the way the `per_share` model does. Confirmed
against real data: **all 250 trades in this session's 6-month papertrade run
paid exactly €0.00 commission.** Spread and slippage were charged correctly —
this is commission-specific. **Not scoped to this one run**: any papertrade
run against the real `data/phantom_ledger` account, on the account's current
`tiered` profile, has been simulating commission-free. Root cause is in the
sibling repo `phantom_ledger` (Baz-owned, `/media/baz/MonkeyWorks/PycharmProjects/phantom_ledger`,
installed here as a dependency) — **not fixed, not yet decided who fixes it**;
see "Immediate next step."

**The naive class gate is still not landed** — it was the stated next step in
the previous handoff, got unblocked mid-session when the sibling's sweep
finished, and then this session's attention moved to the three items above
instead of returning to it. Confirmed at end-of-session:
`resolve_disabled_strategies()` still has no `naive` parameter.

3 commits this session (`5fae762`, `66e46e4`, `fb7fa5e`), none pushed. Tree
clean except the usual pile of untracked handoff docs.

## Suggested skills

- **`read-handoff`** to pick up context (this doc).
- **`cheap-models-first`** before any subagent spawn.
- **`slice-workflow`** if landing the naive class gate as a proper slice.

## The immediate next step

Two candidates, not one — say which, or both:

**A. Land the naive class gate in `resolve_disabled_strategies()`
(`strategy/kairos_strategies.py`).** Fully unblocked now (sibling's sweep is
done, `kairos_strategies.py` is not imported by anything running). Design,
already agreed with Baz two sessions ago: add a `naive: bool = False`
parameter; when set, skip step 1 (the per-group oracle-tested check) entirely
and make step 2's `stage` resolve to `"naive"` instead of
`"finetuned"/"base"`. **The trap, flagged twice by the sibling and worth
repeating a third time:** key the fall-through on the `naive` parameter
itself, never on step 1 returning an empty set — an empty result from step 1
means "oracle-tested and clean," not "no data," and treating it as a miss
would silently class-gate every healthy group. Thread `naive=naive` from
`kairos_signals._run_group`'s existing call site (it already carries a
`naive` flag for the predict_fn selection, `cefa274`).

**B. Decide who fixes the `phantom_ledger` commission bug**, and whether any
past papertrade result that used the real account should be considered
unreliable until it's fixed. Not fixed this session — it's a different repo,
found late, and fixing it wasn't authorized. Exact location:
`phantom/models/broker.py`, `CommissionModel.calculate()`, the `tiered`
branch (~line 26 in the installed package copy under
`.venv/lib/python3.13/site-packages/phantom/models/broker.py` — the real fix
belongs in the `phantom_ledger` source repo, not the installed copy). Two
independent gaps: (1) key mismatch (`up_to`/`rate` vs the config's
`min_volume`/`max_volume`/`per_share`), (2) even with matching keys, the
tiered branch has no `per_share_min` floor logic at all, unlike the
`per_share` branch's `max(self.per_share_min or 0.0, quantity * rate)`.

## What was done this session

1. **Wired `--stage naive` into `kairos_pipeline.py`'s CLI** (`5fae762`).
   `run_stage_naive()` already existed and worked — the sibling's
   `scripts/run_oracle_dedup.py` had been calling it by direct import for the
   whole 961-group sweep — but it was absent from argparse's `--stage`
   choices and the dispatch, so nothing reachable from the CLI itself could
   run it. Folded into `oracle`'s existing branch (`elif args.stage in
   ("oracle", "naive")`) rather than duplicated, matching the file's own
   `("base", "finetuned")` idiom. `--disable_min_signals` deliberately not
   forwarded — naive results never reach the disable gate, and a pre-existing
   guard already rejects that flag for any stage but
   `oracle`/`auto`/`rebuild_disabled`, confirming the choice independently.
   Also corrected `run_stage_naive()`'s docstring, which still described the
   pre-`01aff70` design (reuse oracle's peeking decision) rather than the
   persistence forecast that actually runs.

   **Caught and fixed a real bug in the process, in my own code, not the
   target's**: the manual end-to-end verification of this wiring pointed
   `kp.DB_PATH` at a `/tmp` copy, believing that isolated it — it does not.
   `get_connection(db_path=DB_PATH)` binds its default at function-definition
   time, so reassigning the module attribute afterward changes nothing. The
   test run connected to the real `data/pipeline_results.db` and wrote 36
   `oracle_results` rows, 36 `strategy_class_stats` rows, one `runs` row, and
   a CSV to `results/`, all at `backtest_period='3m'` (`run_id=8369`). The 6m
   corpus was unaffected (zero rows at 6m). Cleaned up (DB rows deleted, CSV
   removed) before it did any damage to the corpus other sessions read. Fixed
   properly in `e55db61`: patches `get_connection` itself, not just
   `DB_PATH`, plus a test that asserts the fixture is actually redirecting.

2. **Ran a real 6-month base papertrade** (`kairos_papertrade.py
   --months-back 6.0 --interval 1d --capital 200`, real `IBKR` account,
   `naive=False`). Result: +€10.03 (+5.01% cash, Sharpe 1.15), 250 trades
   (156 sl / 94 tp), MTM return +2.59% / Sharpe 0.69 / **max drawdown 34.09%**
   (the cash/MTM accounting divergence is expected and documented — see
   CLAUDE.md's MTM section — but the 34% MTM drawdown is real risk
   information regardless of which headline number gets trusted). Discovered
   the commission bug above while sanity-checking this run's numbers, not by
   going looking for it. Class-level breakdown of the run's own 250 trades
   (not sweep-side `strategy_class_stats`): equity +€2.58/58 trades/48.28%
   win, crypto +€3.20/130/41.54%, fx_commodity +€4.25/62/43.55% — every class
   positive, crypto carries the volume but the worst win rate and highest
   cost drag.

   Two monitoring mistakes made and fixed live, both worth remembering: a
   heartbeat grepping one progress-bar pattern went stale for 2h40m without
   the process actually being stuck (the bar it tracked belonged to a phase
   that had already finished; fixed by cross-checking two independent
   signals — CPU time and report-file count — instead of one bar); and a
   completion watcher built as `tail -f | grep "pattern"` with no `-m 1`
   matched the target line but could never exit and would never have
   notified — the run's actual finish was found by manually checking, not by
   a notification arriving. Also found and killed a duplicate instance of
   that same broken watcher (Baz caught "you have a monitor too many").

3. **Built an HTML result artifact** for the papertrade run,
   `results/kairos_papertrade_202609012036_report.html` (gitignored,
   self-contained, no dependencies — invoked the `dataviz` skill first per
   its mandatory trigger). Equity curve as a real hover chart (crosshair +
   tooltip, cross-checked point coordinates against the viewBox before
   calling it done), stat tiles for cash/MTM headline numbers, the
   commission-bug caveat as a prominent banner (not buried in a footnote),
   exit-reason and class-level breakdowns as tile rows. Categorical color
   assignment used the palette's documented first-three-slots (already
   validated all-pairs per the skill's own reference — no need to re-run the
   validator for a 3-category set).

4. **Terminology cleanup in CLAUDE.md** (`66e46e4`, `fb7fa5e`) — Baz-initiated,
   not code-motivated. "Corpus" was used as both a noun (the 961-group
   collection — fine) and an adjective on a stat's *scope* (ambiguous: it
   meant "one group's own row" in one sentence and "pooled across an entire
   class of groups" two sentences later, in the same paragraph). Replaced
   with three named grains: **group-level** (one row per run, in
   `oracle_results`/`model_results`), **class-level** (one row per run split
   by instrument class, in `strategy_class_stats` — still scoped to a single
   run, same as group-level; "group" isn't in the name because every
   non-pooled grain already carries that scope by default), **pooled**
   (aggregated across every run of a stage, optionally filtered to one
   class — the only grain where run-scope is actually collapsed, which is
   why it's the only one that needs to say so). "Corpus" is kept only as a
   noun for the collection itself. Docs-only — `source="corpus"` in
   `kairos_pipeline.py:1415` (a code-level string with three assertions in
   `test_class_stats.py`) was deliberately left alone; renaming it is a
   separate, real code change, not a documentation edit.

## Published artifacts

- `results/kairos_papertrade_202609012036_report.html` — the 6-month
  papertrade result, described above. Gitignored (`results/` is not
  tracked), so it does not exist on any other checkout or after a clean
  clone; if this needs to survive, it needs to be moved somewhere tracked or
  regenerated from `/tmp/papertrade_6m/` (see "Housekeeping" below for that
  directory's lifespan).
- `docs/papers/*` — untouched this session; last regenerated by the sibling
  session at `81a9314` on the completed 961/961 naive sweep. Should still be
  current.

## Git state

2 commits ahead of `origin/master/master` at session start (the sibling's
final two), **26 ahead at session end** — 3 are this session's own
(`5fae762`, `66e46e4`, `fb7fa5e`), the rest are the sibling's, already
described in the previous handoff. **Not pushed.**

```
fb7fa5e Retire the two remaining "corpus"-as-adjective spots
66e46e4 Name the three stat grains explicitly; retire "corpus" as an adjective
e55db61 Actually isolate the pipeline CLI test from the production database
2d66a86 Record the orphan check that actually finds forked pool workers
29aac87 Record the orphan check that finds forked pool workers, and finalise naive figures
81a9314 Regenerate both papers on the completed naive re-sweep, and fix two stale figures
5fae762 Wire --stage naive into the pipeline CLI
```

(`5fae762` through `dcc187e` below it are also this session's, described
above; full list in `git log --oneline`.)

Tree clean except untracked `docs/handoff-*.md` (30+, unpruned — same
housekeeping item as every recent handoff) and the new
`results/kairos_papertrade_202609012036_report.html` (gitignored, expected).

Remote quirk unchanged: the remote is *named* `origin/master`, so
`git log origin/master..HEAD` returns nothing — use `git status -sb`.

Tests: every area touched this session passed (916 in one full sweep of
pipeline/signals/allocation/class_stats/papertrade/selection/candidate/
disabled/naive-marked tests, 0 failures; plus the new
`test_pipeline_stage_naive.py`, 7/7). Full unrestricted suite was not run
this session.

## Concluded threads

- **The sibling "fix naive" session's entire thread** — naive re-sweep
  (961/961), both papers regenerated, 8 orphaned pool workers found and
  killed (by the sibling, after this session flagged them — see previous
  handoff for the full exchange; the sibling's own correction: they were
  from an *aborted* sweep, not the completed one, and were ~350MB not the
  ~2.8GB first estimated, since forked children share copy-on-write pages).
  Nothing more expected from that session.
- **`--stage naive` reachable from the pipeline CLI** — done, tested, see
  above.
- **A stray write to the real `pipeline_results.db`** — caused and cleaned
  up within this session (`run_id=8369`); see item 1 above. Root cause fixed
  so it can't recur the same way.
- **"Can Kairos's papertrade path actually trade the naive baseline?"** —
  this was the previous handoff's headline question; the actual live run and
  a passing set of fill-mechanics checks happened in the session *before*
  this one (see `docs/handoff-202609011511.md`). Not repeated here.

## Open threads / possible next steps

**1. The naive class gate — see "Immediate next step" A.** This is the
   carry-over item from the previous handoff that never got done this
   session either, now for a different reason (attention went to the CLI
   wiring, the papertrade run, and the terminology cleanup instead). Fully
   unblocked.

**2. The `phantom_ledger` commission bug — see "Immediate next step" B.**
   Newly found this session. Affects every past and future papertrade run
   against the real account until fixed. Not yet decided whether Baz wants
   it fixed in this session, in a `phantom_ledger`-focused session, or left
   for now with results treated as commission-free approximations.

**3. Whether to re-run the 6-month papertrade after the commission fix
   lands**, to get a trustworthy P&L number. The current
   `results/kairos_papertrade_202609012036_report.html` should not be quoted
   as a real return estimate — it's missing real trading cost, and its own
   banner says so.

**4. Offline signal replay for selection/allocation tuning — explicitly
   deferred this session, not started.** Baz asked directly whether to run
   `kairos_signal_replay.py`/`kairos_replay_sweep.py` to find better
   selection/sorting rules. Recommendation given and apparently accepted (no
   pushback before the session ended): not yet, because (a) any
   dollar-ranked outcome right now is tuned against a broken cost signal on
   the real-account side, and replay's own flat-fee cost model is a
   *different* approximation again — "optimal" would mean optimal-for-neither;
   (b) replay's own documented overfitting risk (a rule that wins on one
   window needs a second non-overlapping window to trust, then a real
   papertrade run to confirm — which is the commission problem again); (c)
   the naive class gate is a smaller, already-scoped task competing for the
   same next slot. Confirmed the replay data itself isn't a blocker whenever
   this does get picked up: `papertrade_signals`/`papertrade_signals_closure`
   already hold 94,592 precomputed closures, dated 2026-02-01 through
   2026-08-21 (would want extending to cover through the papertrade run's
   2026-09-01 end date before trusting a sweep against the full window).

**5. Housekeeping.**
   - 30+ unpruned handoff docs, unchanged from every previous session's
     note.
   - `/tmp/papertrade_6m/` (~600MB: phantom state is actually in the real
     `data/phantom_ledger/`, not `/tmp` this time — the `/tmp` dir holds the
     183 daily `.md` reports, run log, heartbeat log, and the source HTML
     the published artifact was copied from) — fine to delete once the
     artifact copy in `results/` is confirmed sufficient.
   - `/tmp/naive_pt/` (~530MB, from the previous session's naive-papertrade
     verification) — stale, safe to delete, nothing reads it.
   - `/tmp/pipe_wiring_test.db` (520MB, the throwaway DB copy used to verify
     `--stage naive` dispatch) — stale, safe to delete.
   - Disk: `/media/baz/MonkeyWorks` at 97% (14G free), `/` at 88% (11G free).
     None of the three `/tmp` items above are on `/media/baz/MonkeyWorks`, so
     clearing them helps `/` (currently the tighter of the two), not the
     project drive.

**6. Inherited from earlier handoffs, still untouched:** small/mini model
   sweeps to match base's coverage (`docs/handoff-202609010724.md`'s
   original ask — 922-group base vs. ~200-group small/mini, unrelated to
   this session's papertrade work and not revisited); ~14 groups failing on
   the partial-bar NaN on the model path specifically (oracle-path fix
   exists, model-path doesn't); the stashed chunked-sampling VRAM prototype;
   wave packing committed but never run live; the long-dormant set (offline
   replay persistence — now specifically item 4 above — DEGIRO/BUX, IB
   Gateway login, PREDCACHE-01, Epic E17, `n0`/`min_n` as global constants).
