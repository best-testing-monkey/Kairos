# Handoff — 2026-08-19 10:58

## TL;DR

Two unrelated pieces of work this session, both committed (not pushed, per
standing instruction): (1) added the account-name-collision hard guard
requested from the previous handoff's open-threads list, plus fixed two
genuinely pre-existing, unrelated broken-test issues discovered while
verifying it; (2) investigated and disproved a stale "48 tests broken"
warning in `CLAUDE.md` — the code and tests were both already fine (fixed by
a commit 6 days ago that never got documented), so the fix was doc-only.
**Nothing this session is pushed to remote.**

## What was done this session

### 1. Account-name-collision hard guard (commit `f83c6d4`)

Requested item from `docs/handoff-202608190826.md`'s open-threads #2.
`strategy/kairos_papertrade.py` now refuses to start if `--account-name`
already exists, via a new `_refuse_duplicate_account_name(client,
account_name)` helper called right before `client.accounts.create(...)`
(around line 2123). Raises `RuntimeError` with an explanation of *why*
(AccountRepo.get_by_name() has no ORDER BY, so a duplicate name silently
resolves to whichever row SQLite returns first). New test file
`tests/unit/test_kairos_papertrade_account_name_guard.py` (2 tests, uses the
same real-`phantom`-client fixture pattern as
`test_kairos_papertrade_remove_open_positions.py`) — both pass.

### 2. Fixed pre-existing stale fx-correction tests (commit `ecfc19f`)

Verifying #1 against the full `test_kairos_papertrade.py` file (not just the
new test file) turned up an unrelated, pre-existing break: the file still
imported and tested `compute_corrected_realized_pnl()`, which was **removed**
by commit `f0dece2` (2026-08-17) when `phantom_ledger` fixed the
`fx_conversion_cost`-omission bug upstream (`realized_pnl` is read as-is now,
no client-side correction). That commit updated `kairos_papertrade.py` and
two other test files but missed `test_kairos_papertrade.py` itself — it
couldn't even collect. Also found and fixed a broken relative import
(`from ..allocation import AllocationConfig` → `from allocation import
AllocationConfig`, matching the bare-import convention `conftest.py`'s
`sys.path` insertion sets up for every other `strategy/`-module import in
this file). After both fixes: `TestCorrectedCashFillCloseDelta`,
`TestFillCloseDeltaMarginAware`, and `TestLiquidatePosition`'s stale expected
values (92.0→92.5, 97.0→97.5, -204.5→-205.0 — the exact fx_conversion_cost
that's no longer double-subtracted) were re-derived and updated.
**Deliberately not touched**: `TestPrewarmPredictionCache`'s 11 failures in
this same file — confirmed these test the *old* two-pass (check + load)
prewarm design that `CLAUDE.md` already documents as replaced by a single
inline-checked pass (2026-08-11); a separate, known, larger gap. Full-file
result went from "1 collection error" → "13 failed, 111 passed" → **"11
failed (all TestPrewarmPredictionCache), 113 passed"**.

### 3. Disproved "48 tests broken" in CLAUDE.md (commit `888b859`)

Baz flagged that `CLAUDE.md`'s "Known issues in the WIP commit, not yet
fixed" section (part of the "Overlapping prediction caches..." saga
write-up, WIP commit `4c3659f` dated 2026-08-11) looked wrong and asked for
a doublecheck of whether the code or the tests were actually broken. Ran all
three files it named (`test_predcache.py`, `test_kairos_strategies_model_switch.py`,
`test_predict_all_batch_cache.py`): **62 collected, 62 passed, 0 failures.**
Cross-checked the three specific code claims by reading current source, all
confirmed already fixed:
- `_no_data_fallback_warned` is at module scope in `kairos_strategies.py`
  (not trapped inside `_dist_cache_put()`).
- `PredictionCache.__init__` (`strategy/kairos_predcache.py`) calls
  `os.makedirs()` before constructing `SqliteDict`.
- `_evict_disk_if_over_budget()`/`_disk_write()` were removed outright (not
  patched) — correct, since `put()` no longer writes `.npz` files at all.

Root cause of the stale doc: commit `ee745c7` (2026-08-13, "Fix papertrade
prewarm RAM blowup and hardened OOM watchdog" — see memory
`project_papertrade_2026_08_13_saga.md`) fixed all four issues but never
touched `CLAUDE.md`. Rewrote that section in `CLAUDE.md` to say "RESOLVED as
of `ee745c7`, verified 2026-08-19" instead of a misleading "not yet fixed"
list, with a note-to-future-self not to trust this file's "known issues"
bullets without re-checking git log first. **No code or test changes were
needed for this item — doc-only fix.**

## Environment note (new this session, worth knowing)

The project disk (`/media/baz/MonkeyWorks/...`) was extremely slow this
session — `uv run --with pytest python -m pytest ...` took **10–16 minutes**
per invocation even for small/fast test files (vs. seconds expected). Once
warm, subsequent runs were fast (~40s–4s). If a pytest run seems to hang,
it's very likely legitimate disk/env slowness, not a real infinite loop —
give it 480s+ (foreground Bash calls will auto-background past 120s) before
assuming something's wrong. Not investigated further (unrelated to any code
change); flagging so the next session doesn't waste time chasing it as a bug.

## Memory updated this session

`phantom_ledger` (imported as `phantom`) is confirmed to be Baz's own sibling
repo, same pattern as `price_cache` — source at
`/media/baz/MonkeyWorks/PycharmProjects/phantom_ledger`. Saved to
`project_repos_and_deps.md`: bugs traced into it should be fixed at the
source (bug ticket, or a separate Claude Code instance against that repo
directly) rather than worked around in Kairos.

## Git state

**Kairos**, branch `master` — **3 commits this session, none pushed**:
`f83c6d4` (account-name guard), `ecfc19f` (stale fx-test fix), `888b859`
(CLAUDE.md correction), oldest to newest, on top of the previous session's
`a524dbe`. Working tree clean. Standing instruction: commit freely, push only
when explicitly asked, every time.

Remote naming gotcha (recorded before, still true): remotes are
`origin/master`, `origin/main`, `origin/signals-report` — three separate
remote entries. Push with e.g. `git push "origin/master" master`.

Full-suite run not repeated this session (previous session's handoff already
confirmed 1197 passed / 1 pre-existing unrelated failure as of `a524dbe`);
this session's file-scoped runs (`test_kairos_papertrade.py`,
`test_kairos_papertrade_account_name_guard.py`, and the three predcache
files) all accounted for above.

## Open threads / possible next steps

1. **Everything from `docs/handoff-202608190826.md`'s open-threads #1, #3,
   #4 is still open and untouched this session** (deciding which of the 14
   no-crypto leveraged rules to pursue further; re-running the
   crypto-inclusive 14 rules under margin-utilization-target sizing;
   `liquidate_position` → `ph.positions.close()` migration mentioned in an
   even older handoff). Open-thread #2 (account-name guard) is now done —
   see above.
2. **`TestPrewarmPredictionCache`'s 11 failures** (in
   `test_kairos_papertrade.py`) are still unfixed — they test the superseded
   two-pass prewarm design. Not attempted this session since it's a larger,
   separate, already-documented gap (CLAUDE.md's "Prewarm is now a single
   inline-checked pass, not two" section already describes the replacement;
   the tests just were never updated to match).
3. **The two new commits (`f83c6d4`, `ecfc19f`) and the doc commit
   (`888b859`) have not had an independent code-review pass** — same
   standing recommendation as last session for any pre-push review.
4. Consider whether `CLAUDE.md`'s dated "gotcha" sections in general are
   worth a periodic staleness sweep, given this is now the second time (see
   also `docs/handoff-202608190826.md` item #4 referencing item drift
   across handoffs) that a documented "known issue" turned out to be
   already-fixed-but-undocumented. Not proposed as a concrete task, just
   flagged as a pattern.

## Suggested skills for the next session

- `cheap-models-first` — already loaded and followed this session (mostly
  inline work at the sonnet rung, consistent with the parent model already
  being Sonnet 5); load again before any batch-rerun or multi-rule
  delegation work on open-thread #1.
- `code-review` (medium or high effort) — recommended for the three commits
  from this session before any push, per open-thread #3 above.
- `slice-workflow` — if the next session picks up open-thread #2
  (`TestPrewarmPredictionCache` cleanup) or open-thread #1 (leveraged-rule
  follow-up), both look like spec-able slices this project's standard
  workflow skill would fit well.
