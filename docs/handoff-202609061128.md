# Handoff — 2026-09-06 11:28

## TL;DR

Continues from `docs/handoff-202609021247.md`. This session did **not**
touch that handoff's two open items (the naive class gate, the
`phantom_ledger` commission bug) — both are still exactly where they were.
Instead, prompted by a `/read-handoff` question about a past finding, the
session went: recall the IBKR commission-viability finding → research
alternative brokers/exchanges for a NL-resident account → build a reusable
framework for that research.

**Main deliverable: a broker/exchange cost-discovery probe framework**,
built from scratch this session and fully committed (`c7442ee`):
- `scripts/exchange_probe.py` — shared `ExchangeProbe` Protocol + schema/
  upsert/report/sweep machinery, generalized out of
  `scripts/ibkr_instruments.py` (which now imports it back — net **97
  fewer lines**, **zero test changes**, verified byte-identical `--report`
  output against the real 190-row measured dataset).
- `docs/broker-api-interface.md` — the interface map (Tier 1 discovery,
  built; Tier 2 execution, still undesigned/Phase 5), and the two
  cost-discovery patterns (A: whatIf/dry-run probing, IBKR-only so far; B:
  direct fee-schedule lookup, every ccxt exchange).
- `docs/playbooks/add-exchange-probe.md` — concrete steps + code skeleton
  for adding a new Pattern B exchange.
- `docs/exchanges/{bitvavo,kraken,bybit-eu,finst,alpaca-europe}.md` —
  implementation-ready connection docs, researched via 5 parallel forks.
- `tests/unit/test_exchange_probe.py` — proof test: a fake exchange run
  through the real shared pipeline.

**Two findings from the exchange research that changed the picture**:
**Finst has no public API** (institutional-only, not in ccxt — deprioritize
it), and **Alpaca Europe is Broker-as-a-Service, not a direct trading
account** (Kairos would onboard as its own broker-of-record — materially
heavier than assumed when it was first flagged). Both corrected into
`roadmap/phase-5-auto-execution.md` and memory.

Scope was deliberately capped at scaffolding + docs this session (Baz's
explicit call) — **no exchange accounts exist, nothing has been
sandbox-tested**. That's the natural next step, see "Open threads."

A full unrestricted `pytest tests/unit/` run was started near the end of
the session as a final sanity check beyond the targeted tests already
verified green, and **had not finished by the time this handoff was
written** — check its result before assuming the suite is clean.

1 commit this session (`c7442ee`), not pushed. Tree otherwise matches the
previous handoff (same 22-commits-ahead state, same untracked handoff-doc
pile).

## Suggested skills

- **`read-handoff`** to pick up context (this doc).
- **`cheap-models-first`** before any subagent spawn — this session forked
  5 parallel research agents for the per-exchange docs (cheap since the
  session already runs on Sonnet 5 and forks share its cache/context); that
  pattern is worth repeating for similar fan-out research.
- **`slice-workflow`** if landing the naive class gate (still open, see
  below) as a proper slice.

## What was done this session

1. **Answered a recall question, corrected memory.** Baz asked whether a
   past session had found IBKR's commission/costs too high for realistic
   profit. Confirmed yes — `docs/ibkr-cost-discovery.md` (from a
   2026-09-02 session that never got its own handoff doc, commits
   `cd17ba2`..`34adf28`): flat $1 commission floor, Kairos's ~€18 avg trade
   can't even be placed (below the fractional-share barrier), round-trip
   cost 0.6-0.7%+ at any placeable size vs. the 0.15% the allocation math
   assumes. Updated `project_ibkr_account_constraints.md` memory (was
   stale, mid-session snapshot) with the final currency-corrected figures.

2. **Researched alternative brokers/exchanges** for a NL-resident account,
   crypto included per Baz's ask. Checked internal precedent first
   (roadmap's Binance/Kraken/Bybit/Alpaca/IBKR plan, sibling repo
   `insider_trading_analysis`'s DEGIRO/BUX/IG/etc. availability scrapers),
   then web-searched current (2026) status. Found **Binance is now EU-dead**
   (no MiCA license, shut off EU users 2026-07-01) and **Alpaca launched a
   real European broker in April 2026** (Alpaca Europe) — both updates to
   the existing roadmap. Landed on 4 crypto candidates (Bitvavo, Finst,
   Kraken, Bybit EU) + Alpaca Europe as the equities option. Saved as
   `project_broker_exchange_candidates.md` memory (later rewritten in step
   4 below with the deeper findings).

3. **Scoped the follow-up build with two clarifying questions** (asked via
   AskUserQuestion, not assumed): refactor depth — scaffolding+playbook
   only, no live sandbox tests yet, but keep the exchanges in an open
   thread as more are found; doc scope — the 4 crypto exchanges **plus**
   Alpaca Europe (5 total, not 4).

4. **Built the framework** (see TL;DR for the file list). Sequence:
   read `scripts/ibkr_instruments.py` in full to identify genuinely
   generic vs. IBKR-specific logic → wrote `scripts/exchange_probe.py` →
   refactored `ibkr_instruments.py` to reuse it (thin delegating wrappers
   for `upsert`/`connect_db`/`structurally_terminal`/`already_done`,
   `_num`/`infer_commission_model` re-exported) → ran existing tests
   (15 passed unchanged, 7 skipped — no `ib_async` in the base env, same as
   before) → smoke-tested `--report` against the real DB (output
   byte-identical to pre-refactor) → wrote `tests/unit/test_exchange_probe.py`
   (a `FakeExchangeProbe` proving the shared `sweep_simple()`/`upsert`/
   `report` pipeline works end-to-end for a new exchange) → wrote the two
   reference docs → updated `roadmap/phase-5-auto-execution.md` (crossed
   off Binance, flagged Alpaca's real status, pointed at the new docs) →
   forked 5 parallel research agents (one per exchange, each inheriting
   this session's full context) for `docs/exchanges/*.md` → reviewed all 5
   on return, corrected two places (`docs/broker-api-interface.md`,
   `roadmap/phase-5-auto-execution.md`) where the Finst/Alpaca findings
   contradicted what had been written before those forks reported back →
   added a CLAUDE.md pointer section → rewrote the
   `project_broker_exchange_candidates.md` memory with the full findings
   table and the explicit open-thread instruction → committed (`c7442ee`)
   → kicked off a full `pytest tests/unit/` run as a final check (see TL;DR
   — still running as of this doc).

   A design decision worth knowing if this is picked up again: **the two
   cost-discovery mechanisms (IBKR's dry-run probing vs. a direct
   fee-schedule call) were deliberately NOT forced through one generic
   sweep loop.** `sweep_simple()` only covers Pattern B (the 3 real crypto
   candidates); IBKR keeps its own bespoke `sweep_one()`, documented as the
   Pattern A reference shape in the playbook rather than retrofitted into
   the shared abstraction. Tried designing it the other way first and
   dropped it — forcing both patterns into one shape either required real
   surgery on `sweep_one` (risky, it produced trusted real data) or would
   have duplicated its logic in a second place.

## Published artifacts

None this session (no Artifact tool publishes) — all output is
repo-committed markdown/Python, listed above.

## Git state

```
c7442ee Add a reusable broker/exchange cost-discovery probe framework
34adf28 Apply final currency-corrected margin figures from kairos-07's sweep
048acd7 Replace the measured figures with the currency-corrected full sweep
d219fae Make --report show costs per class and flag uncorrected margin rows
b3affce Don't re-probe structurally terminal rows under --force
```

`git status -sb`: `master...origin/master/master [ahead 22]` — 1 new this
session (`c7442ee`), 21 pre-existing from before (see previous handoffs).
**Not pushed.** Remote quirk unchanged: the remote is *named*
`origin/master`, so `git log origin/master..HEAD` returns nothing — use
`git status -sb`.

Tree clean except the untracked `docs/handoff-*.md` pile (31 files now
including this one, unpruned across many sessions — same standing
housekeeping item every recent handoff has noted).

Tests: `tests/unit/test_ibkr_instruments.py` + `tests/unit/test_exchange_probe.py`
together — 20 passed, 7 skipped (missing `ib_async` in the base env, same
as before this session's changes). A full `pytest tests/unit/ -q` run was
started afterward as a broader check and **had not completed** when this
session ended — its result is unknown, check it before trusting "full
suite green" for anything in this repo right now.

## Concluded threads

- **The IBKR-commission recall question** — answered, memory corrected.
- **Broker/exchange candidate research** — done; superseded by item 4's
  deeper per-exchange findings (Finst no API, Alpaca is B2B).
- **The broker/exchange probe framework (interface map, refactor, 5 docs,
  playbook)** — fully built, tested, documented, committed this session.
  This was this session's main ask and is complete as scoped (scaffolding
  + docs only, explicitly not live-account-tested — see open threads).

## Open threads / possible next steps

**1. Sign up for real accounts/sandbox keys and run a live probe.** Explicit
   Baz instruction: keep this open, and **keep adding newly-found exchanges
   to this list** as more are discovered. Cheapest first target per this
   session's research: **Bybit EU** has a real free testnet (48h API-key
   unblock wait, no funded account needed) — see
   `docs/exchanges/bybit-eu.md`. Bitvavo and Kraken need a real account
   (Kraken has no self-service spot sandbox at all). Finst is deprioritized
   (no public API — would need to email `institutional@finst.com` first).
   Alpaca Europe is blocked on resolving the B2B-vs-individual onboarding
   question before anything else there is worth doing — see
   `docs/exchanges/alpaca-europe.md`'s open questions.

**2. The naive class gate — carried over from `docs/handoff-202609021247.md`,
   still not landed, still fully unblocked.** Add a `naive: bool = False`
   parameter to `resolve_disabled_strategies()`
   (`strategy/kairos_strategies.py`); when set, skip step 1 (the per-group
   oracle-tested check) entirely and make step 2's `stage` resolve to
   `"naive"` instead of `"finetuned"/"base"`. The trap flagged multiple
   times now: key the fall-through on the `naive` parameter itself, never
   on step 1 returning an empty set (empty means "oracle-tested and clean,"
   not "no data"). Thread `naive=naive` from `kairos_signals._run_group`'s
   existing call site (`cefa274`).

**3. The `phantom_ledger` commission bug — carried over, still not fixed,
   still not decided who fixes it.** `CommissionModel.calculate()`'s
   `tiered` branch (in the `phantom_ledger` sibling repo,
   `phantom/models/broker.py`) reads keys `up_to`/`rate`; the real IBKR
   account profile uses `min_volume`/`max_volume`/`per_share`. Every
   papertrade run against the real account has been simulating €0.00
   commission as a result. Note this is now doubly superseded in
   importance by this session's finding
   ([[project-ibkr-account-constraints]]) that IBKR's *real* commission
   structure likely erases Kairos's edge at its trade size regardless —
   fixing the simulator bug would make results *worse* (more realistic),
   not better, so there's an argument this matters less urgently now that
   IBKR itself looks like the wrong venue. Worth raising explicitly next
   session rather than silently deprioritizing.

**4. Whether IBKR is worth continuing to build against at all**, given (2)
   and (3) above plus this session's exchange research — a live
   conversation-starter, not a coded task. If a crypto exchange (Bitvavo/
   Kraken/Bybit EU) looks more viable once probed for real (open thread 1),
   that could shift where Phase 5 execution work actually goes, ahead of
   fixing IBKR-specific issues in `phantom_ledger`.

**5. Full test suite result unknown** — see "Git state" above. Check
   `pytest tests/unit/ -q` cleanly finishes before trusting the repo's test
   health beyond the two files this session specifically verified.

**6. Inherited from earlier handoffs, still untouched**: small/mini model
   sweeps to match base's coverage; ~14 groups failing on the partial-bar
   NaN on the model path specifically; the stashed chunked-sampling VRAM
   prototype; wave packing committed but never run live; offline signal
   replay for selection/allocation tuning (deferred, not started); the
   long-dormant set (DEGIRO/BUX account signup, IB Gateway login,
   PREDCACHE-01, Epic E17, `n0`/`min_n` as global constants);
   housekeeping (31 unpruned handoff docs; `/tmp/papertrade_6m/`,
   `/tmp/naive_pt/`, `/tmp/pipe_wiring_test.db` — check whether these still
   exist before assuming they do, this note is now two sessions old).
