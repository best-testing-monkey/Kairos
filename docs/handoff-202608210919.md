# Handoff — 2026-08-21 09:19

## TL;DR

Kairos's multi-interval (1d→1h) rollout is functionally done: E0 and epics
E10–E16 are implemented, live-verified against real data, and committed
(latest: `48801a0`). E17 (hourly Telegram digest + scheduling) is not
started. Separately, mid-verification a real bug was found and chased down:// crypto symbols report `$vol=0.0` in 1h universe screening, root-caused to
yfinance's intraday crypto volume data omitting volume on ~50% of individual
hourly bars (confirmed via raw yfinance repro, bypassing price_cache
entirely — this is **not** a price_cache/Kairos computation bug in the
BUG-04 sense, just sparse upstream data). User chose the fix ("option 1":
resample dollar-volume to daily buckets and sum before taking the median).
**That fix is implemented, uncommitted, and mid-verification right now** —
this is the very next thing to finish. See "Open threads" below.

## Suggested skills

- None required to continue the in-progress fix — it's a small, well-scoped
  change already implemented. Read `strategy/kairos_pipeline.py`'s
  `compute_universe_stats`/`evaluate_liquidity` and the two test files
  listed below to pick up the exact diff.
- If moving on to **E17** next: no `/breakdown` doc exists for it yet
  (E10–E17 were broken down together from
  `docs/tickets/DESIGN_DOC_multi_interval_1h.md`, but only E10–E16 have
  been run through `/run-stories`). Just run `/run-stories E17` — the
  ticket file should already exist under `docs/tickets/` from the earlier
  `/breakdown` pass; check there first before writing a new one.
- If moving on to the **actual 1h discovery sweep** (finding real
  profitable signal/instrument combos, as opposed to verifying the
  mechanism): read `docs/playbooks/hourly-discovery-sweep.md` first — it's
  a from-scratch guide written this session specifically for that, not
  something to re-derive.

## What was done this session

- Completed E0 (shared multi-interval plumbing: Sharpe annualization,
  `BARS_PER_DAY` consolidation, interval-aware signals cache key,
  interval-filtered correlation `run_id` lookups) and epics E10–E16 of the
  1d→1h rollout via `/breakdown` + `/run-stories`, each with a live
  verification against real data and a `docs/playbooks/hourly-*.md`
  instructions doc.
- Found and fixed 3 real bugs via live verification, each with its own
  follow-up ticket and commit:
  - **BUG-03**: DST-ambiguous-time crash in `kairos/data.py`'s local
    fallback fetch (`tz_localize` needed `ambiguous="infer"`).
  - **BUG-04**: the *same* DST crash existed upstream in price_cache's
    primary fetch path, plus a separate crypto-intraday tz mislabeling bug
    (yfinance returns crypto bars in UTC, price_cache blindly treated them
    as already-NY-time). This lives in a sibling repo (`price_cache`, via
    `phantom_ledger`'s submodule) — handed off to a peer Claude Code
    session working that repo per user instruction, fixed upstream
    (price_cache commit `72bac58`), then propagated through: phantom_ledger
    submodule bump+push → `uv lock --upgrade-package price-cache
    --upgrade-package phantom-ledger` (must upgrade both together — one
    alone silently no-ops) → `uv sync`. Hit and worked around an NTFS
    `UV_CACHE_DIR` "Directory not empty" bug along the way — workaround
    (`export UV_CACHE_DIR=/tmp/uv-cache-kairos`) is now documented in
    `CLAUDE.md` and should be used for any `uv lock`/`uv sync`/`uv run` in
    this repo going forward.
  - **E14-S02**: `select_finetune_candidate`'s `already_registered` check
    was interval-blind (compared only `assets`, not `(assets, interval)`),
    which would have silently skipped a real 1h finetune candidate whose
    assets already had a 1d finetuned model.
- Explained the MTM `daily_financing()` mechanism to the user (why it read
  0.0 in the E16-S02 verification run: no positions were still open at a
  day-close snapshot during that window — entry day counts, exit day
  doesn't, same-day round trips never accrue).
- Built and published an HTML papertrade report artifact
  (`https://claude.ai/code/artifact/d67076b5-a260-422f-b7f4-5dba1b56d3e6`)
  from the E16-S02 run's real `phantom.db` data. **User pushback**: this
  report was "underwhelming" — it verified mechanism correctness (MTM/margin
  math fires right), not the actual goal (profitable, persistent
  signal/instrument combinations). This pushback is why
  `docs/playbooks/hourly-discovery-sweep.md` exists — see below.
- Wrote `docs/playbooks/hourly-discovery-sweep.md` (committed `48801a0`):
  explains why E10–E17 answers "does the plumbing work," not "which 1h
  strategies are profitable," and gives concrete directions for actually
  running `--stage auto` at scale, checking persistence via
  `kairos_signal_replay.py`, and confirming with a longer live papertrade
  run — this is the on-ramp to the next, larger, not-yet-started piece of
  work.
- Precisely defined, reproduced, and root-caused the `$vol=0.0` crypto/1h
  liquidity bug (see TL;DR) and implemented the user's chosen fix. Full
  detail in "Open threads" below since this is unfinished.

## Published artifacts

- Papertrade E16-S02 verification report (HTML, sage/teal "ledger paper"
  design, honest framing incl. 0% win rate shown plainly):
  `https://claude.ai/code/artifact/d67076b5-a260-422f-b7f4-5dba1b56d3e6`
  (favicon 📒). Source file (if it needs regenerating/updating):
  `/tmp/claude-1000/-media-baz-MonkeyWorks-PycharmProjects-Kairos/bdfcdb31-c69b-42f6-a87e-bdee92864b1b/scratchpad/papertrade_1h_report.html`
  — that scratchpad path is session-specific and may no longer exist; treat
  the published artifact URL as the durable copy.

## Git state

Branch `master`, HEAD `48801a0`. Working tree has 3 uncommitted, modified
files (not new) — the in-progress `$vol=0.0` fix:

```
 M strategy/kairos_pipeline.py
 M tests/unit/test_pipeline.py
 M tests/unit/test_pipeline_auto.py
```

`git diff --stat`: 115 insertions, 67 deletions across the three files.
Nothing is staged. See "Open threads" for exactly what the diff contains
and what's left before it should be committed. Do not discard this diff —
it's real, reviewed-in-progress work, not scratch.

## Concluded threads

- Full E0 + E10–E16 multi-interval rollout (mechanism-level correctness at
  1h) — done, committed, each epic independently live-verified.
- BUG-03, BUG-04 (both DST/tz bugs, one local, one upstream in price_cache)
  — fully resolved end-to-end, including the cross-repo dependency chain.
- E14-S02 interval-blind finetune candidate bug — fixed, tested, committed.
- MTM `daily_financing()` explanation — answered, no follow-up needed.
- Papertrade report artifact — published; superseded in purpose by the
  `hourly-discovery-sweep.md` playbook (the report answered "does the
  mechanism work," the playbook is the roadmap to answering "is it
  profitable").
- Problem definition + root cause of `$vol=0.0` — fully nailed down (see
  TL;DR). Only the **fix's verification and landing** is still open, not
  the diagnosis.

## Open threads / possible next steps

**1. Finish landing the `$vol=0.0` fix (immediate next step, in progress).**
The fix ("option 1", user's explicit choice): `compute_universe_stats()` in
`strategy/kairos_pipeline.py` now resamples `close*volume` to daily buckets
via `.resample("1D").sum()`, drops zero-sum days, and takes the median
across the remaining days — producing an always-already-daily-equivalent
`dollar_volume` regardless of interval. `evaluate_liquidity()`'s previous
`BARS_PER_DAY`-based scaling of a per-bar dollar_volume was removed
accordingly (kept, it would have double-scaled an already-daily figure).
Test files were updated to match (see `git diff` for exact detail — not
reproduced here per this doc's "don't duplicate other artifacts" rule).

Remaining steps, in order:
1. A full-suite test run (`export UV_CACHE_DIR=/tmp/uv-cache-kairos && uv
   run --with pytest python -m pytest tests/unit/ -q`) was kicked off
   right before this handoff was written and did not finish in time to
   include its result here — **check its outcome first** (background task,
   should be far along or done by the time you read this; if the session
   that started it is gone, just re-run it fresh, it's not expensive). Only
   the pre-existing, known-unrelated
   `TestRunStageFinetuneNextNotifications::test_no_telegram_flag_suppresses_all_notifications`
   failure is expected; anything else is new and must be investigated
   before proceeding.
2. Live-verify against real data:
   `uv run ./strategy/kairos_pipeline.py --stage universe --interval 1h`
   and confirm BTC-USD/ETH-USD/etc. now show nonzero, plausible `$vol` and
   pass the liquidity gate where warranted — this was the original observed
   symptom, so this is the real acceptance check, not just green tests.
3. Commit the change (this repo's convention: feed the local Kairos
   `master` branch directly, commit-then-ask-before-push per user's
   established preference — see project memory `feedback_commit_dont_push`).
4. Update `docs/todo.md`'s BUG-04 entry — it currently has a note flagging
   `$vol=0.0` as "not yet investigated"; mark it resolved with a one-line
   pointer to the fix commit.
5. Update `docs/playbooks/hourly-discovery-sweep.md`'s "Prerequisite: check
   BUG-04's residual finding first" section (lines ~29-47 as of this
   writing) — it currently tells the reader to go investigate this as a
   blocker; once fixed, either shrink it to a one-line "already fixed,
   verify crypto passes at healthy numbers" note or remove the prerequisite
   framing entirely.
6. Consider (not required) whether `strategy/kairos_pipeline.py`'s
   `compute_universe_stats` deserves its own `CLAUDE.md` "Known gotchas"
   entry, matching this repo's pattern for other non-obvious
   root-caused bugs — judgment call, only if the root cause (sparse
   upstream crypto volume data) seems likely to bite again elsewhere.

**2. Epic E17 (hourly Telegram digest + scheduling) — not started.**
Two stories (E17-S01/S02) per the original `/breakdown` pass; ticket files
should already exist under `docs/tickets/`. Straightforward continuation of
the `/run-stories` pattern used for E10–E16.

**3. The actual 1h discovery sweep — not started, open-ended.**
`docs/playbooks/hourly-discovery-sweep.md` is the guide; running it for
real (`--stage auto --intervals 1h` at scale, then `kairos_signal_replay.py`
for persistence-checking, then a longer live papertrade confirmation) is
deliberately scoped as separate, larger work — not part of
`docs/todo.md`'s rollout epics. Worth doing only once the `$vol=0.0` fix
above is confirmed live (the playbook's own prerequisite check), since
crypto being liquidity-starved would otherwise silently thin out the sweep.
