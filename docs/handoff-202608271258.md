# Handoff — 2026-08-27 12:58

## TL;DR

This session ran in parallel with a sibling session ("Kairos new symbol sweeps") that was monitoring the live `--stage oracle` sweep described in `docs/handoff-202608260857.md`. Two sidequests were raised: (1) what happens when strategies run with no model signal at all — **parked, needs refinement, there's a misunderstanding to resolve first** (don't pick this up without re-discussing with Baz); (2) the oracle sweep was only using ~4 of 16 cores at a time — **fully resolved**: root-caused (mostly GIL-bound Python, not BLAS), fixed with a process-pool parallel rewrite, benchmarked at a **measured 3.78x speedup** (4 workers vs. sequential, matched groups), and shipped as the new `scripts/run_oracle_dedup.py` (defaults to 8 workers), replacing the old scratchpad sequential script of the same name. Committed (`76a5bbe`) and pushed. The sibling session has already switched the live sweep over to it — **verified running right now: 8 parallel `kairos_strategies.py` subprocesses, each pinned to ~99% CPU (1 core)**, exactly as designed.

One mistake happened and was owned/fixed inline: while chasing what looked like an orphaned test process, this session killed a legitimate live-sweep subprocess (group 7421, `AMZU,BLK,JEF,UPRO`) — the sweep's own error handling absorbed it cleanly (logged `[FAIL]`, moved on), but that group has no results and needs a manual one-off re-run later (exact repro command in "Open threads" below). The sibling session was notified with restore steps at the time.

## Suggested skills

None required to continue monitoring the (now-parallel) oracle sweep — it's the sibling session's job, not this thread's. If sidequest 1 (no-model-signal baseline) gets un-parked, no skill needed to scope it, but `slice-workflow` is this repo's standard loop if it turns into a real implementation. If picking up the international-symbol NaN bug (see below), `slice-workflow` also applies since it's a real correctness bug in `strategy/kairos_pipeline.py` or `kairos_strategies.py`.

## What was done this session

1. **Read the latest handoff** (`docs/handoff-202608260857.md`) and verified live state: oracle sweep had progressed from 65/961 (as documented) to 647/961, one real failure since (group 3972, LSE symbols, `ValueError: cannot convert float NaN to integer`), base-priority sweep confirmed not running (as expected).
2. **Sidequest 1 (no-model signal) — investigated, then parked.** Clarified a real misunderstanding: `--no-prediction` (already used by the `oracle` stage) is NOT a "no signal" baseline — it replaces the model's predicted distribution with the **actual realized next-bar OHLCV** (perfect foresight / oracle ceiling), per `kairos_strategies.py:1041-1042` and `kairos_orchestrator.py:940-941`. What Baz is actually asking about (a floor baseline where strategies get zero distributional signal) isn't built — `dist` is assumed non-None by nearly every `generate_signal()`. Baz said this needs refinement before proceeding — **do not build anything for this without revisiting the ask first.**
3. **Sidequest 2 (CPU imbalance) — root-caused, fixed, benchmarked, shipped:**
   - Confirmed via `ps` that the sequential sweep dispatches one group at a time (`run_backtest_subprocess()` in `kairos_pipeline.py:1188`, blocking `subprocess.run()` in a straight loop in the scratchpad `run_oracle_dedup.py`).
   - Initial hypothesis (BLAS multi-threading using ~4 cores per subprocess, unparallelized across groups) was refined mid-session after Baz's live observation ("still seeing one core maxed") — the real bottleneck is mostly single-threaded Python (day-by-day, strategy-by-strategy loop, GIL-bound); the "~4 cores average" in `ps` was brief vectorized bursts blended into a cumulative average, not sustained BLAS parallelism. This *strengthens* the case for multiprocessing (separate GILs) over just BLAS thread-tuning.
   - Built `scripts/run_oracle_parallel.py` (later renamed, see below): `ProcessPoolExecutor`-based worker pool, one `kairos_strategies.py` subprocess per worker, each pinned to `OMP_NUM_THREADS=1`/`OPENBLAS_NUM_THREADS=1`/`MKL_NUM_THREADS=1`/`NUMEXPR_NUM_THREADS=1` to prevent oversubscription; same resume/skip logic as the original script (checks `oracle_results` before running); own sqlite connection per worker with WAL mode + 60s timeout for concurrent-write tolerance.
   - **Verified wiring** with a 5-group all-skip dry run, then a real single-group execution test.
   - **Incident:** while hunting an "orphaned" process from an earlier killed test, mistakenly killed the live sweep's actual current subprocess (group 7421). Sweep self-recovered; sibling session notified with a restore snippet (see Open threads).
   - **Ran a controlled, paired benchmark** at Baz's request (sweep paused by Baz via the sibling session first): 4 matched 4-asset groups serial/unthrottled (`1057.1s` total, ~264s/group) vs. 4 matched 4-asset groups parallel/4-workers/1-thread-each (`279.4s` total) — **3.78x speedup**, close to linear. Individual per-group wall-clock barely changed under 1-thread cap (264s → ~272s), confirming the GIL-bound diagnosis.
   - Cleaned up one leftover stopped process from an earlier crashed benchmark attempt before handing back.
   - Told sibling session benchmark was done, machine was clear, safe to resume — sibling resumed using the new parallel script per instruction, then confirmed live: "all cores revving now."
4. **Finalized and shipped the parallel script per Baz's explicit request:**
   - Set `--workers` default to `8` (was `cpu_count()-2`=14; 8 matches physical core count on this box — 8 physical/16 logical, SMT doesn't reliably double GIL-bound throughput).
   - Renamed `scripts/run_oracle_parallel.py` → `scripts/run_oracle_dedup.py` (same name as the original scratchpad script it replaces — a deliberate identity swap, not a coincidence), updated docstring accordingly.
   - Verified with `--help` after rename.
   - Committed (`76a5bbe`) and pushed to `origin/master master` (this repo's remotes are literally named `origin/main`, `origin/master`, `origin/signals-report` — not the usual bare `origin` — see `git remote -v`; push accordingly, not `git push origin master`).
   - Notified sibling session of the rename/new default so its next invocation (or a restart) uses the right command (`uv run scripts/run_oracle_dedup.py 760`, no `--workers` needed).
5. **Verified end state**: live `ps` check at end of session shows **8 parallel `kairos_strategies.py` subprocesses actively running**, each pinned to ~98-99% CPU (1 core) — confirms the sibling session did switch over and the fix is working in production, not just in the benchmark.

## Published artifacts

None (no Artifact tool calls this session).

## Git state

Branch `master`, HEAD `76a5bbe`, pushed to `origin/master master` (remote is up to date — verified via push output `d303854..76a5bbe`). One new commit since `docs/handoff-202608260857.md`'s `a50c41b`:
- `76a5bbe` — `scripts/run_oracle_dedup.py` (new file, 129 lines): parallel process-pool oracle sweep, defaults `--workers=8`, replaces both the never-committed scratchpad sequential script and this session's intermediate `scripts/run_oracle_parallel.py` (renamed before commit, so that filename never existed in git history).

Working tree clean except the same pile of untracked `docs/handoff-*.md` files (now 23, still unpruned — see Open threads).

**Not committed (unchanged from before, by design):** `data/ibkr_shortable/*.csv`, `normalize_to_yfinance.py` (gitignored). This session's own scratchpad test scripts (`bench_serial.py`, `bench_parallel.py`, `test_one_group.py`) live in this session's own scratchpad dir, not the repo — nothing to clean up in-repo.

## Concluded threads

- **Sidequest 2 (CPU imbalance in the oracle sweep)** — fully concluded: root-caused, fixed, benchmarked (3.78x @ 4 workers), shipped as `scripts/run_oracle_dedup.py` (committed, pushed), and confirmed live in production by the sibling session.
- **"Is `--no-prediction` already a no-model-signal switch?"** — concluded as a clarification, not a build: no, it's the oracle perfect-foresight baseline, not a floor baseline. (The underlying sidequest 1 itself is NOT concluded — see Open threads.)
- **Group 7421 accidental-kill incident** — concluded as an owned mistake with a handed-off fix: sibling session notified with exact restore steps at the time; restore itself is still pending (see Open threads, item 1).
- **Machine-sharing coordination during the benchmark** — concluded cleanly: Baz had the sibling pause the sweep, this session benchmarked, cleaned up its own stray process, and handed back cleanly; sibling resumed with the new script per instruction.

## Open threads / possible next steps

**1. Restore group 7421's oracle results — needs a manual one-off run.** Not automatic; the sweep only walks its group list once and won't revisit a failed group. Once the current parallel sweep run completes (or anytime, it's independent), run:
```python
import sys; sys.path.insert(0, "strategy")
import kairos_pipeline as kp
conn = kp.get_connection(kp.DB_PATH)
kp.run_stage_oracle(conn, ["AMZU", "BLK", "JEF", "UPRO"], interval="1d", backtest_period="6m", pred_samples=100, disable_min_signals=5)
```
Or simpler: just re-run `scripts/run_oracle_dedup.py 760` after the sweep finishes — its exists-check will skip everything already done and only attempt real gaps (7421 plus anything else that failed).

**2. Sidequest 1 (no-model-signal baseline) — parked, needs re-scoping with Baz before any work.** Baz said there's a misunderstanding to resolve. This session's read: `--no-prediction` gives strategies perfect foresight (ceiling), not zero signal (floor) — the floor experiment (`dist=None` or a flat/uninformative distribution fed to every strategy) isn't built and would need each strategy's `generate_signal()` to degrade gracefully rather than assume a real `dist`. Don't build anything here until Baz clarifies what he actually wants.

**3. International-symbol NaN bug — real, recurring, not yet root-caused.** Hit three times this session/last: group 3972 (`AAL.L,BOL.ST,GLEN.L,RIO.L`), and during benchmark group selection, `CX,EWG,EWW,GMEXICOB.MX` and `0268.HK,1211.HK,2318.HK,7226.HK` were also avoided for the same reason (not confirmed to fail, but same symbol-suffix pattern as 3972, deliberately sidestepped rather than tested). Error is `ValueError: cannot convert float NaN to integer` inside the `kairos_strategies.py` subprocess when run with `--no-prediction` on groups containing `.L`/`.HK`/`.MX`/`.ST`-suffixed (non-US) symbols. The oracle sweep's own error handling absorbs this as a `[FAIL]` and moves on, so it's not blocking, but it means an unknown number of international groups are silently missing oracle coverage. Worth root-causing (likely related to the currency/window-sizing fixes from the prior session, `docs/handoff-202608260857.md` item 2) before calling the oracle stage complete. A completeness pass (`SELECT DISTINCT` assets in `oracle_results` vs. the full `select_deduped_groups(760)` list) would surface the true scope.

**4. Oracle sweep — live right now, driven by the sibling session, using the new parallel script.** Not this session's job to babysit (sibling owns monitoring), but worth knowing: as of this handoff, 8 parallel groups confirmed running via `ps` (each ~99% CPU / 1 core, matching the thread-pinning design). No ETA computed for the parallel run yet — the sequential run's `eta_min` field doesn't carry over; the sibling session's own log (`stage_oracle3.log` or a new log if it restarted output) is the place to check current progress/ETA.

**5. `--stage base` priority sweep — unchanged from prior handoff, still paused pending oracle coverage.** `scripts/run_base_priority.py` is committed and ready; Baz's plan is to launch it once oracle has meaningfully more coverage (no exact threshold given).

**6. `--stage finetuned`** — still deliberately not built out, needs Baz to name a checkpoint. Unchanged from prior handoff.

**7. Everything from `docs/handoff-202608260857.md`'s item 4 not touched this session, still open:** offline `kairos_signal_replay.py` persistence disagreement vs. real papertrade result; DEGIRO/BUX broker-account path; live IB Gateway account-login issue; BA/COST full-3-month persistence; PREDCACHE-01; `viability_report`/`finetuned_models` registry drift; Epic E17.

**8. Housekeeping — 23 handoff docs now, still unpruned.** Same standing advice as every prior handoff: safe to prune to the most recent 1-2 without asking, whenever a session has a free moment. Nobody has done this yet across many sessions — if picking this up, just do it rather than flagging it again.
