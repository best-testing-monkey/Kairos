# Handoff — 2026-09-06 16:46

## TL;DR

Continues directly from `docs/handoff-202609061513.md`. Two threads this
session-portion: (1) a subagent diagnosed the 9 pre-existing
`test_kairos_papertrade_leverage_regression.py` failures flagged in that
handoff — **not a bug**, just GPU-lock contention with a sibling
session's legitimate scheduled job, no code fix needed; (2) Bitstamp's ID
verification cleared, so it got the same live-validation treatment OKX
and Saxo already had, plus a universe-coverage measurement against
Kairos's 62-symbol crypto universe.

**Headline results:**
- **Test failures diagnosed, not fixed (no fix needed).** All 9 failures
  in `test_kairos_papertrade_leverage_regression.py` share one root
  cause: `kairos_papertrade.main()`'s `GpuLock()` blocked 300s trying to
  flock `/tmp/kairos_gpu.lock`, held by a sibling session's
  `scripts/kairos_weekly_discovery.py` (PID 427566, running since
  ~06:19, still running as of this doc — check before re-running tests).
  `git diff 34adf28..HEAD` touches zero files in `strategy/`, `config/`,
  or lockfiles, ruling out both "regression" and "stale test" theories
  from the prior handoff. **Action: re-run the test file once that PID
  exits.**
- **Bitstamp: LIVE-TESTED AND CONFIRMED VIABLE**, third exchange (after
  Saxo/OKX) to get real API calls this week. Sandbox key confirmed
  environment-locked (rejected on production, `403 API key not found`).
  Fees confirmed live: **0.30% maker / 0.40% taker**, matching
  pre-measurement research exactly — no flat floor. €10.00 EUR minimum
  order size confirmed live. This also resolved the doc's one open
  ambiguity: the API genuinely uses the Advanced fee schedule, not a
  pricier Instant Buy/Sell spread.
- **Bitstamp universe coverage measured: 49/62 (79%)** of Kairos's crypto
  universe is tradeable against EUR on Bitstamp, directly or via free
  conversion — narrower than OKX's 87%. Only 4 new candidates clear
  Kairos's existing volume floor (CASHCAT, ZORA, FET, HYPE); FET and HYPE
  overlap with OKX's own candidate list.
- **Methodology correction, now a standing rule**: Baz caught that the
  first coverage pass undercounted `ZEC` as unusable because it only has
  a `ZEC/USD` pair on Bitstamp — but `EUR/USD` is itself a liquid
  Bitstamp pair, so EUR→USD→ZEC is a free route. Saved as
  `feedback_exchange_coverage_indirect_routes.md` — applies to every
  future exchange coverage check (Bybit EU, Kraken, Bitvavo).

**Credentials handling** (same discipline as OKX/Saxo): Bitstamp sandbox
API key + secret were pasted into the conversation by Baz and used live
via a throwaway scratchpad script, env vars only. Grepped the repo for
both values before every commit — clean each time. Never written to any
repo file. **Not reproduced in this handoff either.**

3 commits this session-portion (`3eee9ef`..`f307427`), all on `master`,
**none pushed**.

## Suggested skills

- **`read-handoff`** to pick up context (this doc + `docs/handoff-202609061513.md`
  and `docs/handoff-202609061158.md` for the full exchange-probe arc).
- **`cheap-models-first`** before any subagent spawn.
- If Bybit EU verification clears and Baz wants the same live-validation
  treatment, no new skill needed — follow the OKX/Saxo/Bitstamp pattern
  (get credentials, throwaway scratchpad script with env vars, grep
  before commit, update the exchange doc + README + memory).

## What was done this session

1. **Investigated the 9 leverage-regression test failures via a Sonnet
   subagent** (carried over as open thread #2 from the prior handoff).
   Root cause: every failure blocks on `GpuLock()` in
   `strategy/kairos_papertrade.py:2084`, which raises `OpsError` after a
   300s flock timeout on `/tmp/kairos_gpu.lock` — a sibling session's
   `scripts/kairos_weekly_discovery.py` (PID 427566) held it via an
   internal `kairos_pipeline.py --stage auto` subprocess. Corroborating
   evidence: the 2 tests that *do* pass call internal functions directly,
   skipping `GpuLock` entirely. `git diff 34adf28..HEAD` confirmed zero
   changes to `strategy/`, `config/`, or lockfiles since the commit named
   as a suspect in the prior handoff — ruling out both a real regression
   and stale test expectations. **No code changed; nothing to fix.**

2. **Bitstamp live-tested via real sandbox credentials Baz provided**
   (API key/secret, `sandbox.bitstamp.net`) — used only as environment
   variables to a throwaway scratchpad script. 6 real calls: public
   ticker (`GET /api/v2/ticker/btceur/`), account balances (auth check),
   trading fees (all pairs + BTC/EUR individually), public
   trading-pairs-info (order size/precision), and the same signed
   balance call repeated against `www.bitstamp.net` to confirm
   environment-locking (rejected, `403`). Updated `docs/exchanges/bitstamp.md`
   and `docs/exchanges/README.md`. Committed `3eee9ef`.

3. **Bitstamp universe coverage measured** against Kairos's 62-symbol
   crypto universe (`kairos_pipeline.CANDIDATE_UNIVERSE["crypto"]`), using
   Bitstamp's public `trading-pairs-info` (257 pairs, no auth). First pass:
   48/62 EUR-quoted (one via a legacy ticker — `RENDER-USD` is listed as
   `RNDR/EUR`), 13 confirmed genuinely absent under any of Bitstamp's 10
   quote currencies. New candidates: checked all 59 non-universe EUR pairs
   live for 24h volume — most are zero-volume; only CASHCAT, ZORA, FET,
   HYPE clear the ~8,139 EUR/24h floor (same floor OKX's pass used).
   Created the "Bitstamp" section in `docs/universe-expansion-candidates.md`.
   Committed `b32ed99`.

4. **Coverage figure corrected after Baz's feedback.** `ZEC` only has a
   `ZEC/USD` pair on Bitstamp — the first pass counted this as "not
   directly usable." Baz pointed out `EUR/USD` is itself a liquid
   Bitstamp pair, so EUR→USD→ZEC is a free conversion route and should
   count as coverage. Revised to **49/62 (79%)**. Also re-checked all
   non-EUR-only quoted bases for new candidates (same conversion logic)
   — found only `ETH2`/`SGD`/`XSGD`/`USDG` (staking derivative, fiat,
   tokenized fiat, stablecoin — none are real directional-prediction
   candidates), so the candidate list didn't change. Committed `f307427`.
   Saved the correction as a standing methodology rule:
   `feedback_exchange_coverage_indirect_routes.md`.

5. **Memory updated throughout**: `project_broker_exchange_candidates.md`
   (Bitstamp findings, corrected coverage figure, description/frontmatter),
   `MEMORY.md` index lines, and the new
   `feedback_exchange_coverage_indirect_routes.md`.

## Published artifacts

None this session (no Artifact tool publishes) — all output is
repo-committed markdown plus memory files.

## Git state

```
f307427 Correct Bitstamp coverage: ZEC is reachable via free EUR/USD conversion
b32ed99 Measure Bitstamp's crypto universe coverage against Kairos's 62 symbols
3eee9ef Confirm Bitstamp fees and env-locking with a live sandbox test
```

`git status -sb`: `master...origin/master/master [ahead 36]`. **Not
pushed.** Remote quirk unchanged: the remote is *named* `origin/master`,
so `git log origin/master..HEAD` returns nothing — use `git status -sb`.

Tree clean except the untracked `docs/handoff-*.md` pile — 42 files match
that glob now (34 untracked per `git status`, implying some older ones
are already committed), unpruned across many sessions, same standing
housekeeping item every recent handoff has noted.

No test suite was run this session-portion beyond the one targeted
subagent investigation (single file, not the full suite).

## Concluded threads

- **9 leverage-regression test failures (prior handoff's open thread #2)**
  — diagnosed: environmental lock contention, not a bug. Residual action
  (re-run once the sibling job finishes) carried into open threads below,
  since it wasn't actually re-run yet.
- **Bitstamp fee/connectivity viability** — settled, confirmed viable
  (0.30%/0.40%, no flat floor, €10 min order).
- **Bitstamp universe coverage** — measured and documented, including the
  methodology correction for indirect conversion routes.

## Open threads / possible next steps

**1. Re-run `test_kairos_papertrade_leverage_regression.py` once
   `scripts/kairos_weekly_discovery.py` (PID 427566 as of this doc,
   holding `/tmp/kairos_gpu.lock`) finishes.** Check `ps -p 427566`
   first — if it's gone, the lock is free and the file should just pass.
   No code change expected to be needed.

**2. Bybit EU verification still pending** (~3 days ID verification,
   reported in progress as of the prior handoff). Once cleared, repeat
   the OKX/Saxo/Bitstamp live-validation pattern: real credentials from
   Baz, throwaway scratchpad script (env vars only), grep before every
   commit, confirm environment-locking, update
   `docs/exchanges/bybit-eu.md` + `docs/exchanges/README.md` + memory,
   and run the same universe-coverage check (apply
   `feedback_exchange_coverage_indirect_routes.md`'s rule from the start
   this time, not as a correction afterward).

**3. Formalize OKX and/or Bitstamp into the real `ExchangeProbe`
   framework** (`scripts/okx_instruments.py` / `scripts/bitstamp_instruments.py`,
   per `docs/playbooks/add-exchange-probe.md`). Still not started — two
   exchanges are now validated (up from one at the last handoff), so the
   abstraction has more to prove itself against.

**4. Run the deduped-group re-screening pipeline against
   `docs/universe-expansion-candidates.md`'s candidates** (38 from OKX +
   4 from Bitstamp, with FET/HYPE overlapping across the two) — per
   `project_crypto_commodity_universe_expansion` memory's stated order:
   universe screening → correlation/grouping → oracle + naive → base →
   small/mini → regenerate `docs/papers/`. **TRX (TRON)** still flagged
   as worth checking first — a top-15 market-cap coin missing from the
   universe for no identified reason.

**5. Whether IBKR is worth continuing to build against at all** — now
   even more pointed with two crypto exchanges (OKX, Bitstamp) confirmed
   cheaper with no flat floor. Still just a conversation-starter, not a
   coded task.

**6. The `phantom_ledger` commission bug** — carried over, still unfixed,
   lower urgency given IBKR itself looks like the wrong venue regardless.

**7. The naive class gate** — carried over from
   `docs/handoff-202609021247.md`, still not landed, still fully
   unblocked.

**8. Inherited from earlier handoffs, still untouched**: small/mini model
   sweeps to match base's coverage; ~14 groups failing on the partial-bar
   NaN on the model path specifically; the stashed chunked-sampling VRAM
   prototype; wave packing committed but never run live; offline signal
   replay for selection/allocation tuning (deferred, not started); the
   long-dormant set (DEGIRO/BUX account signup, IB Gateway login,
   PREDCACHE-01, Epic E17, `n0`/`min_n` as global constants); housekeeping
   (42 handoff docs now, most unpruned — fifth+ session running on this
   note; `/tmp/papertrade_6m/`, `/tmp/naive_pt/`, `/tmp/pipe_wiring_test.db`
   — check whether these still exist before assuming they do).
