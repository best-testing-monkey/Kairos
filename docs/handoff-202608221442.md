# Handoff — 2026-08-22 14:42

## TL;DR

Continuing from `docs/handoff-202608221229.md`. The first full 3-month whitelist papertrade run (from that handoff) completed with **`total_profit_eur=-18.24 EUR (-9.12%), sharpe=-3.72, num_trades=2551`**, but its `mtm_*` metrics were badly corrupted (`mtm_total_return_pct=-99.99%`). Root-caused, fixed, tested, and committed as `03c2958`: **BUG-11** — the day-loop's same-day-round-trip cash-reconciliation scan re-lists every closed position each hourly iteration and re-applies its fill+close cash delta if it closed "today," so with `--interval 1h` a same-day round trip gets its P&L multiplied by however many hourly iterations remain in that calendar day. Confirmed by reverting the fix and reproducing the exact corruption in a new regression test (`test_same_day_round_trip_not_recounted_across_hourly_iterations`), then confirming the fix restores correctness. Full suite: 1713 passed.

Baz then pointed out a second, deeper concern: the corrupted equity tracker also feeds order-sizing (`admission_snapshot.equity`), so a crashed-toward-zero tracked equity would silently starve position sizing too (`quantity <= 0` skips silently) — meaning the reported `num_trades=2551`/P&L from the FIRST run may itself be an undercount of what the whitelist strategy would actually do with correct equity tracking, not just its MTM curve being wrong. Baz asked for a fresh 3-month run with the fix applied, pinned to the same start hour as this morning's run (`--effective_per "20260822 0811"`) specifically to hit the persistent prediction cache and skip re-computing model inference. **This re-run is in progress now** (account `kairos_papertrade_202608220811_bug11fix`, real ledger, no `--phantom-data-dir` isolation) and is running dramatically faster than the first run thanks to the cache hit — interval-report passes that took ~22 minutes each this morning now take ~1-2 seconds.

## Suggested skills

None needed — next step is purely "wait for the re-run, then report corrected numbers to Baz."

## What was done this session (since the 12:29 handoff)

**Papertrade run #1 completed**: `total_profit_eur=-18.24, pct_profit=-9.12%, sharpe=-3.72, num_trades=2551`. Report: `results/kairos_signals_papertrade_202608220800_202605230100_1h_3.0m.json`.

**Investigated the wildly divergent MTM numbers** (`mtm_total_return_pct=-99.99%` vs. the real `-9.12%` cash P&L). Found exactly one `cash reconciliation gap` warning in the run's log (181.74 EUR gap between phantom's real cash and Kairos's day-loop `corrected_cash`), matching the documented, expected divergence pattern in `docs/tickets/DESIGN_DOC_mtm_margin_leverage.md` §4.2 — but the MTM curve's near-total-wipeout scale meant something beyond the "expected" divergence was also happening.

**Root-caused BUG-11** by reading `strategy/kairos_papertrade.py`'s day loop (~line 2290 area, the same-day-round-trip scan first introduced for BUG-01/BUG-02). Confirmed: the scan does `client.positions.list(account_name, status="closed")` and filters to positions closed "today" on EVERY iteration, with no tracking of which such positions were already credited in an earlier iteration of the same day. For `--interval 1d` this never mattered (one iteration per day); for `--interval 1h` (used by this whitelist run), a same-day round trip gets re-credited on every remaining hourly iteration of that day.

**Fixed (commit `03c2958`)**: added `same_day_round_trip_seen_date`/`same_day_round_trip_seen_ids` tracking, reset when the calendar day advances, so a given closed-position id is only ever credited to `corrected_cash` once per day regardless of iteration count.

**Added and verified a regression test**: `test_same_day_round_trip_not_recounted_across_hourly_iterations` in `tests/unit/test_kairos_papertrade_leverage_regression.py` — same TICKA/TICKB scenario as the existing `test_same_day_round_trip_mixed_with_multiday_position`, but day1 split into four hourly iterations instead of one daily iteration. Asserted the outcome must be byte-identical to the daily-interval sibling test's pinned values. **Proved the test actually catches the bug**: temporarily `git stash`'d the fix, re-ran the new test, confirmed it fails with exactly the predicted corruption (day2 cash showed a 1784.10 EUR gap = 3× the 594.70 round-trip P&L, from 3 spurious re-counts), then restored the fix (`git stash pop`) and re-confirmed it passes.

**Full suite run**: 1713 passed (1712 pre-existing + 1 new), 483.57s, 0 failures.

**Baz raised a second concern** (not yet independently re-verified, but plausible and worth taking seriously): `margin_rejected_count=0` and `phantom_fill_rejected_count=0` in run #1's metadata, yet Baz observed "a lot of positions weren't opened because of low funds" — this is consistent with BUG-11 corrupting `admission_snapshot.equity` (built from the same broken `corrected_cash`) toward near-zero mid-run, which would starve `quantity = alloc_eur / entry` down to `<= 0` and skip order placement via a SILENT `continue` (line ~2191, no counter/log) — a code path that wouldn't show up in either rejection counter. **Not yet confirmed this actually happened in run #1's log** — worth checking (e.g. counting `quantity <= 0` skips, which currently aren't logged at all — might need a temporary instrumentation pass to verify retroactively) if the re-run's post-fix `num_trades` comes back suspiciously similar to 2551.

**Launched the fixed re-run**: `--months-back 3.0 --interval 1h --capital 200 --min_ev_pct 0.10 --signal-selection <18-combo whitelist rule> --no-telegram --effective_per "20260822 0811" --account-name kairos_papertrade_202608220811_bug11fix`. Pinning `--effective_per` to run #1's exact window-end timestamp was Baz's explicit ask, specifically to hit `kairos_predcache`'s persistent disk cache (`data/predcache/`) and skip re-running Kronos model inference for the whole 3-month/18-combo sweep. Confirmed working: prewarm went from a cold ~4.7s/req to ~79 req/s within the first minute, and the "Interval report" backtest passes (which took ~21-22 minutes each in run #1) are now completing in ~1-2 seconds each. Used a distinct `--account-name` (not a `--phantom-data-dir` override) since `_refuse_duplicate_account_name()` hard-blocks reusing an account name by design (a documented anti-footgun, not an oversight) and Baz wants this against the real ledger, not an isolated sandbox. A `Monitor` (task `bp2aaddzp`) is armed on the new log (`whitelist_3mo_papertrade_v2.log`) for completion/error signals.

## Published artifacts

None new this segment.

## Git state

Branch `master`, HEAD `03c2958` ("Fix BUG-11: dedupe same-day round-trip cash credits across --interval 1h iterations") — one new commit since the 12:29 handoff. Working tree: only untracked `docs/handoff-*.md` files (now 16, this one makes 17) — no modified/staged files.

## Concluded threads

- **BUG-11 root-caused, fixed, tested, committed.** The MTM-metrics corruption from run #1 is now understood and resolved for any future run.
- Run #1's headline P&L (`-18.24 EUR/-9.12%`) — reported to Baz, understood to be correct (phantom-ledger-derived, unaffected by BUG-11) but now **superseded in relevance** by Baz's follow-up concern that the SAME underlying corrupted equity tracker may have suppressed trade volume via silent under-sizing, meaning even this "correct" number might not reflect what the strategy would really do with the bug fixed.

## Open threads / possible next steps

**1. The bugfixed 3-month re-run is in progress — this is the main open item, and should resolve fast given the cache hit** (likely single-digit minutes total, vs. run #1's ~4h20m, though the exact total isn't yet known since only the backtest-loop speedup has been observed so far — prewarm-phase model loading for the same 10 groups still has to happen, unless those are ALSO fully cache-hit; watch for it either way). **Next agent's first action**: check `/tmp/claude-1000/-media-baz-MonkeyWorks-PycharmProjects-Kairos/bdfcdb31-c69b-42f6-a87e-bdee92864b1b/scratchpad/whitelist_3mo_papertrade_v2.log` for a `✅ Kairos papertrade finished: ...` line (or the armed `Monitor` task `bp2aaddzp` may have already surfaced it as a notification). Report `total_profit_eur`, `pct_profit`, `sharpe`, `num_trades`, and this time ALSO the `mtm_*` fields (which should now be trustworthy) to Baz.

**2. Directly compare run #2's `num_trades` against run #1's 2551.** If they're close, that's evidence BUG-11 did NOT meaningfully suppress trade volume (the "low funds" observation Baz made may instead trace to something else — e.g. the 322 stale-bracket rejections already seen in run #1, or genuine margin-utilization capping, both legitimate/working-as-designed). If run #2's `num_trades` is meaningfully HIGHER, that confirms Baz's hypothesis that BUG-11-corrupted equity was silently starving order sizing too, which would be a separate, still-unfixed finding worth investigating (specifically: whether the silent `quantity <= 0` skip at kairos_papertrade.py's order-building loop, ~line 2191, needs its own counter/logging so this class of issue is visible next time, the way `margin_rejected_count`/`phantom_fill_rejected_count` already are for the other two rejection paths).

**3. Housekeeping — now 17 handoff docs accumulated in `docs/`.** Flagged in every handoff since yesterday 19:32 and still not actioned. Safe to prune to the most recent one or two whenever convenient.

**4. Unchanged from prior handoffs, none touched this segment:** whether BA/COST (the 7-day replay's equity standouts) persist over the full 3-month window; the 4-combo curated replay vs. naive top-3-everything allocation; re-running `--stage auto`/`--stage base` for equities now that BUG-10 is fixed; JTO-USD manual finetune re-queue; IBKR-sourced universe expansion (blocked — gateway not active); PREDCACHE-01 ticket; `viability_report`/`finetuned_models` registry drift; Epic E17.

**5. Standing note, unchanged:** the `handoff` skill's `disable-model-invocation` flag was deliberately removed (Baz's explicit request) so its 2-hourly cron trigger works during unattended/autonomous sessions.
