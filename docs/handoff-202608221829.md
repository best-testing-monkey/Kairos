# Handoff — 2026-08-22 18:29

## TL;DR

Continuing from `docs/handoff-202608221629.md`. The bugfixed 3-month whitelist papertrade re-run (`kairos_papertrade_202608220811_bug11fix`) **completed**. Corrected result: **-79.83 EUR (-39.91%), Sharpe -3.79, 2551 trades**, with MTM metrics now internally consistent (`mtm_total_return_pct=-38.96%`, roughly matching cash P&L, unlike run #1's nonsensical `-99.99%`). This is a materially WORSE, more honest number than run #1's buggy `-18.24 EUR/-9.12%` — BUG-11 was silently shrinking position sizing over the course of the run (not suppressing trade count, which came out identical at 2551 in both runs), masking the true magnitude of the loss. Reported to Baz along with a direct answer to his "low funds" observation: trade count didn't change, sizing did — confirming his hypothesis about a sizing-related symptom, though the mechanism was equity-shrinkage rather than orders being skipped outright. **Bottom line delivered to Baz: the 18-combo whitelist strategy set is a clear loser over the real 3-month window** (-39.91%), contradicting the offline signal-replay persistence check that originally motivated building it. No background process running; session is idle, awaiting Baz's next direction.

## Suggested skills

None needed.

## What was done this session (since the 16:29 handoff)

**Re-run completed** (elapsed ~4h from launch, consistent with run #1's simulation-phase pace once the prewarm cache-hit speedup is accounted for). Result: `total_profit_eur=-79.82545478089811, pct_profit=-39.91%, sharpe=-3.785, num_trades=2551, mtm_total_return_pct=-38.96%, mtm_max_drawdown_pct=40.97%, mtm_sharpe=-3.46, mtm_liquidation_events=0, mtm_ruined=false`. Report: `results/kairos_signals_papertrade_202608220800_202605230100_1h_3.0m.json`.

**Compared run #1 (buggy) vs. run #2 (fixed) side by side:**
| | Run #1 (buggy) | Run #2 (fixed) |
|---|---|---|
| Cash P&L | -18.24 EUR (-9.12%) | -79.83 EUR (-39.91%) |
| Sharpe | -3.72 | -3.79 |
| Trades | 2551 | 2551 |
| MTM return | -99.99% (broken) | -38.96% (sane) |
| Cash-reconciliation gap | 181.74 EUR | -1.91 EUR |
| Stale-bracket rejections | 322 | 322 |

**Answered Baz's "low funds" hypothesis directly, using the identical-trade-count evidence.** Since `num_trades=2551` in BOTH runs, BUG-11 did NOT suppress order placement/trade count (ruling out the silent `quantity <= 0` skip theory raised in the 14:42 handoff). What actually happened: BUG-11 dragged the day-loop's tracked `corrected_cash`/equity toward zero over the run (feeding `admission_snapshot.equity`, which position sizing scales against), so the SAME 2551 trades executed but at progressively smaller sizes in run #1 — understating the true EUR loss by more than 4x. Confirmed by the near-zero cash-reconciliation gap in run #2 (-1.91 EUR, vs. run #1's 181.74 EUR) and the unchanged stale-bracket count (322 in both), which rules out stale-bracket rejection differences as an explanation.

**Delivered the final verdict to Baz**: the 18-combo whitelist (identified via the offline `kairos_signal_replay.py` persistence check across earlier session segments) is a clear loser in real 3-month live simulation (-39.91%), directly contradicting the optimistic offline persistence-check result that motivated building it in the first place. This is the deliverable Baz originally asked for ("let's do a 3 month papertrade... with only the non sparse profitable combos... to see which stay winning") — answered, definitively, in the negative.

**Flagged a minor housekeeping fact**: the report JSON filename is derived only from window dates (`kairos_signals_papertrade_202608220800_202605230100_1h_3.0m.json`), not account name, so run #2's report silently overwrote run #1's file on disk. Run #1's numbers are only preserved in this conversation and in `docs/handoff-202608221229.md`/`docs/handoff-202608221442.md` — not recoverable from disk.

**Cleanup**: stopped the now-unneeded `Monitor` task (`bp2aaddzp`) that was watching the re-run's log.

## Published artifacts

None new this segment. `results/kairos_signals_papertrade_202608220800_202605230100_1h_3.0m.json` now holds run #2's (corrected) numbers.

## Git state

Branch `master`, HEAD `03c2958` — unchanged since the 14:42/16:29 handoffs, no commits this segment. Working tree: only untracked `docs/handoff-*.md` files (now 18, this one makes 19) — no modified/staged files.

## Concluded threads

- **The bugfixed 3-month whitelist papertrade run** — completed, reported, and understood. This closes the long-running thread that started with Baz's 18-combo whitelist request several handoffs ago.
- **Baz's "low funds" hypothesis** — confirmed as a real symptom of BUG-11, root-caused precisely (equity-shrinkage-driven under-sizing, not order-count suppression), using the identical-trade-count comparison as direct evidence.
- **The whitelist strategy set's real-world viability** — definitively answered: it loses money (-39.91% over 3 months), contradicting the offline persistence check. This is a real, substantive finding Baz should factor into future signal-selection decisions, not just a bug-hunting footnote.

## Open threads / possible next steps

**1. Why does the offline `kairos_signal_replay.py` persistence check disagree so sharply with the real `kairos_papertrade.py` outcome?** This is now the most interesting and highest-value open question, not yet investigated. Candidates worth checking, roughly in order of likely impact: (a) the offline replay tool's cost model is flat/simplified vs. phantom's real per-instrument model (already documented as a known divergence in CLAUDE.md's "Offline Signal Replay" section — "results are directional signals for testing selection rules, not P&L predictions"); (b) the 322 stale-bracket rejections (same-day price drift between the cached signal-generation mirror and the live fill-day price) have no analog in the offline replay tool at all, and may be disproportionately filtering out the specific whitelisted combos' best setups; (c) real position-sizing/margin-utilization competition across `top_n=3` slots shared by 18 mostly-single-ticker combos, which the offline replay's simpler allocation model may not reproduce faithfully. Worth a real investigation before trusting the offline replay tool's persistence-check methodology for future whitelist decisions.

**2. Should Baz try a DIFFERENT combo selection, tighter selection criteria, or abandon the whitelist-restriction approach entirely** given this result? Not yet raised with Baz as an explicit next step — worth surfacing as a question in the next session if he doesn't raise it himself.

**3. Housekeeping — now 19 handoff docs accumulated in `docs/`.** Flagged in every handoff since 2026-08-21 19:32 and still not actioned. Genuinely worth pruning now — this is a long streak of un-actioned advice. Safe to keep just the most recent one or two.

**4. Unchanged from prior handoffs, none touched this segment:** whether BA/COST (the 7-day replay's equity standouts) persist over the full 3-month window; the 4-combo curated replay vs. naive top-3-everything allocation; re-running `--stage auto`/`--stage base` for equities now that BUG-10 is fixed; JTO-USD manual finetune re-queue; IBKR-sourced universe expansion (blocked — gateway not active); PREDCACHE-01 ticket; `viability_report`/`finetuned_models` registry drift; Epic E17.

**5. Standing note, unchanged:** the `handoff` skill's `disable-model-invocation` flag was deliberately removed (Baz's explicit request) so its 2-hourly cron trigger works during unattended/autonomous sessions.

**6. Communication-style note for whoever picks this up**: during this segment, Baz gave direct feedback about monitoring-update pacing — when told a background run is "close to a race" (e.g. approaching a memory-kill threshold), follow through with frequent check-ins rather than a single dramatic update and then silence. Applied for the remainder of this run's monitoring.
